# Copy this file to .env and adjust the values for your environment.
# Any variable prefixed with SHELL_MCP_ overrides defaults in src/shell_mcp/config.py.

# Docker image used for executing run_code snippets.
SHELL_MCP_DEFAULT_IMAGE=python:3.11-slim

# Working directory inside the container.
SHELL_MCP_CONTAINER_WORKDIR=/workspace

# Allow outbound network access by default (true/false).
SHELL_MCP_DEFAULT_NETWORK=true

# Optional resource guards (uncomment to enforce stricter per-run limits).
# SHELL_MCP_MEMORY=512m
# SHELL_MCP_CPUS=1
# SHELL_MCP_READONLY_FS=true

# Run containers under a specific host user/group (uncomment if needed).
# SHELL_MCP_USER_ID=1000
# SHELL_MCP_GROUP_ID=1000

# Docker image pull policy: if-not-present | always.
SHELL_MCP_PULL_POLICY=if-not-present

# Maximum concurrent run_code executions.
SHELL_MCP_MAX_CONCURRENCY=3

# Host directory where successful scripts are stored.
SHELL_MCP_SAVED_SCRIPTS_DIR=/Users/hal/projects/mcp/scripts/

# Legacy compatibility envs (only needed if migrating old setups).
# MCP_DOCKER_IMAGE=python:3.11-slim
# MCP_CONTAINER_WORKDIR=/workspace
