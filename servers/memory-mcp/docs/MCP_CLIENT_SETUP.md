# 🔧 Настройка MCP клиента

## Обзор

Для работы с MCP сервером Memory MCP необходимо настроить MCP клиент (например, Cursor, Claude Desktop, или другие совместимые клиенты).

## 🚀 Быстрая настройка

### Автоматическая генерация конфигурации

```bash
# Запустите скрипт для генерации конфигурации
./scripts/generate_mcp_config.sh
```

Скрипт автоматически:
- Определит путь к проекту
- Найдёт виртуальное окружение
- Сгенерирует правильную конфигурацию MCP

## 📋 Ручная конфигурация для Cursor

Добавьте в файл `~/.cursor/mcp.json`:

### 🎯 Рекомендуемая конфигурация (с виртуальным окружением)

```json
{
  "mcpServers": {
    "memory-mcp": {
      "command": "/Users/hal/projects/mcp/servers/memory-mcp/.venv/bin/python",
      "args": ["-m", "memory_mcp.mcp.server"],
      "cwd": "/Users/hal/projects/mcp/servers/memory-mcp"
    }
  }
}
```

**Важно:** Замените `/Users/hal/projects/mcp/servers/memory-mcp` на ваш реальный путь к проекту!

### 🔄 Альтернативный вариант (с активацией venv)

```json
{
  "mcpServers": {
    "memory-mcp": {
      "command": "bash",
      "args": [
        "-c", 
        "cd /Users/hal/projects/mcp/servers/memory-mcp && source .venv/bin/activate && python -m memory_mcp.mcp.server"
      ]
    }
  }
}
```


## 🚀 Проверка работы

После настройки MCP клиента:

1. **Перезапустите MCP клиент** (Cursor, Claude Desktop и т.д.)
2. **Откройте чат с AI**
3. **Проверьте доступные инструменты** - должны появиться:

### Универсальные инструменты
- `search` - универсальный поиск (hybrid, smart, embedding, similar, trading)
- `batch_operations` - пакетные операции (update, delete, fetch)
- `graph_query` - запросы к графу знаний (neighbors, path, related)
- `background_indexing` - управление фоновой индексацией (start, stop, status)
- `summaries` - управление саммаризациями (update, review)
- `ingest` - индексация данных (records, scraped content)

### Специализированные инструменты
- `index_chat` - индексация конкретного Telegram чата
- `get_available_chats` - список доступных чатов
- `get_statistics` - статистика системы
- `search_explain` - объяснение результатов поиска
- `generate_embedding` - генерация эмбеддингов
- `health` - проверка состояния сервера
- `version` - информация о версии и возможностях

### Торговые сигналы
- `store_trading_signal` - сохранение торгового сигнала
- `search_trading_patterns` - поиск торговых паттернов
- `get_signal_performance` - метрики производительности сигнала

## 🔍 Примеры использования

### Универсальный поиск
```
Найди все сообщения о криптовалютах используя гибридный поиск
```

Или через MCP инструмент:
```json
{
  "search_type": "hybrid",
  "query": "криптовалюты",
  "top_k": 10
}
```

### Smart Search (интерактивный поиск)
```
Найди обсуждения про Bitcoin с уточнением запроса
```

### Индексация чата
```
Проиндексируй чат "Believers Community" с умной агрегацией
```

### Запросы к графу знаний
```
Найди связи между узлами "Bitcoin" и "Ethereum" в графе знаний
```

### Получение статистики
```
Покажи статистику по всем проиндексированным чатам
```

### Объяснение результатов поиска
```
Объясни, почему этот результат релевантен запросу "криптовалюты"
```

## 🛠️ Устранение неполадок

### Проблема: MCP сервер не запускается

**Решение:**
1. Проверьте путь к проекту в конфигурации
2. Убедитесь, что виртуальное окружение активировано
3. Проверьте установку зависимостей: `pip install -e .`

### Проблема: Инструменты не появляются

**Решение:**
1. Перезапустите Cursor
2. Проверьте логи MCP сервера
3. Убедитесь, что сервер запускается без ошибок

### Проблема: Ошибки импорта

**Решение:**
1. Активируйте виртуальное окружение: `source .venv/bin/activate`
2. Переустановите зависимости: `pip install -e .`
3. Проверьте PYTHONPATH

## 📝 Полный пример конфигурации

```json
{
  "mcpServers": {
    "memory-mcp": {
      "command": "/Users/hal/projects/mcp/servers/memory-mcp/.venv/bin/python",
      "args": ["-m", "memory_mcp.mcp.server"],
      "cwd": "/Users/hal/projects/mcp/servers/memory-mcp",
      "env": {
        "PYTHONPATH": "/Users/hal/projects/mcp/servers/memory-mcp"
      }
    }
  }
}
```

## 🎯 Преимущества каждого подхода

### Локальный запуск с venv (stdio режим)
- ✅ Быстрый запуск
- ✅ Использует установленные зависимости
- ✅ Легко отлаживать
- ✅ Минимальное потребление ресурсов
- ✅ Прямая интеграция с MCP клиентами

### HTTP режим (Docker/продакшен)
- ✅ Масштабируемость
- ✅ Изоляция окружения
- ✅ Легкое развертывание
- ✅ Доступен для других сервисов по сети

## 📚 Дополнительная информация

- **Подробная документация по инструментам:** [`AGENTS.md#mcp-инструменты`](../AGENTS.md#mcp-инструменты)
- **Архитектура системы:** [`ARCHITECTURE.md`](ARCHITECTURE.md)
- **Примеры использования:** см. раздел "Сценарии использования" в [`AGENTS.md`](../AGENTS.md)

---

**Рекомендация:** Используйте локальный запуск с виртуальным окружением (stdio режим) для разработки и тестирования. HTTP режим рекомендуется для продакшена и интеграции с другими сервисами.
