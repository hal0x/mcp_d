# 🚀 Telegram Dump Manager

[![Python 3.8+](https://img.shields.io/badge/python-3.8+-blue.svg)](https://www.python.org/downloads/)
[![Code style: black](https://img.shields.io/badge/code%20style-black-000000.svg)](https://github.com/psf/black)
[![Ruff](https://img.shields.io/endpoint?url=https://raw.githubusercontent.com/astral-sh/ruff/main/assets/badge/v2.json)](https://github.com/astral-sh/ruff)
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)

Современная система для управления дампами Telegram чатов с поддержкой нейросетевого анализа, саммаризации и векторного поиска.

## 🔌 Сетевая топология

| Service | Port | Transport | Dependencies |
|---------|------|-----------|--------------|
| `binance-mcp` | `8000` | `stdio` | `redis`, `postgres` |
| `tradingview-mcp` | `8060` | `streamable-http` | `redis`, `postgres`, `binance-mcp`, `memory-mcp` |
| `memory-mcp` | `8050` | `stdio` | `redis` |
| `shell-mcp` | `8070` | `stdio` | `redis` |
| `backtesting-mcp` | `8082` | `stdio` | `postgres`, `binance-mcp`, `tradingview-mcp` |

> Memory MCP предоставляет MCP-интерфейс по адресу `0.0.0.0:8050` и доступен для других сервисов по `http://memory-mcp:8050`.

## ✨ Возможности

- 🧠 **Двухуровневая индексация** - умная группировка сообщений в сессии с автоматической саммаризацией
- ⚡ **Инкрементальная индексация** (NEW!) - индексация только новых сообщений при повторных запусках
- 🔍 **Векторный поиск** - поиск по сообщениям, сессиям и задачам с помощью эмбеддингов
- 🚀 **Гибридный поиск** (NEW!) - BM25 + Vector search с Reciprocal Rank Fusion для улучшенной точности
- 💡 **SearchExplainer** (NEW!) - объяснение результатов поиска с декомпозицией scores и трассировкой связей
- ⭐ **Importance scoring** (NEW!) - автоматическая оценка важности сообщений
- 🗑️ **Memory pruning** (NEW!) - интеллектуальная очистка памяти на основе eviction scores
- 📊 **Кластеризация сессий** (NEW!) - автоматическая группировка похожих сессий в темы (HDBSCAN)
- 🕸️ **Типизированный граф** (NEW!) - граф знаний с сущностями, событиями и связями
- 📝 **Автоматическая саммаризация** - создание структурированных Markdown отчётов через Ollama
- ✅ **Извлечение задач** - автоматическое выделение action items и TODO из разговоров
- 🔁 **Отслеживание дублей** - анализ повторяющихся сообщений и их вариаций
- 🔗 **Кросс-анализ** - поиск корреляций и связей между чатами
- 🧠 **Граф инсайтов** - построение графа знаний и выявление скрытых связей
- 🌐 **MCP сервер** - интеграция с AI клиентами
- 🎯 **Индексация чатов** (NEW!) - индексация конкретных чатов с отслеживанием прогресса
- 🎯 **CLI интерфейс** - удобный командный интерфейс
- ⚡ **Высокая производительность** - оптимизированная обработка больших дампов

## 📁 Структура проекта

```
tg_dump/
├── 📂 src/memory_mcp/                    # Основной код проекта
│   ├── core/                         # Ядро системы
│   ├── analysis/                     # Анализ и обработка данных
│   ├── search/                       # Поиск и индексация
│   ├── memory/                       # Управление памятью и граф знаний
│   ├── quality_analyzer/             # Анализ качества
│   ├── cli/                          # Командный интерфейс
│   └── utils/                        # Утилиты
├── 📂 docs/                          # Документация
│   ├── reports/                      # Отчёты и анализы
│   └── *.md                         # Документация
├── 📂 scripts/                       # Скрипты и утилиты
├── 📂 examples/                      # Примеры использования
├── 📂 tests/                         # Тесты
├── 📂 config/                        # Конфигурационные файлы
├── 📂 chats/                         # Дампы Telegram чатов
├── 📂 artifacts/                     # Артефакты и результаты
│   ├── reports/                      # Сгенерированные отчёты
│   ├── smart_aggregation_state/      # Состояние умной агрегации
│   └── chroma_db/                    # Векторная база данных
└── 📂 .github/                       # GitHub Actions и шаблоны
```

📖 **Подробное описание структуры:** [`docs/PROJECT_STRUCTURE.md`](docs/PROJECT_STRUCTURE.md)

## 🚀 Быстрый старт

### Установка

```bash
# Клонирование репозитория
git clone https://github.com/memory_mcp/memory_mcp.git
cd memory_mcp

# Установка в режиме разработки
make install-dev

# Или обычная установка
pip install -e .
```

### Настройка Ollama

#### Локальная установка
```bash
# Запуск Ollama
ollama serve

# Установка моделей
ollama pull gemma3n:e4b-it-q8_0
ollama pull mxbai-embed-large:latest
```


### Использование

```bash
# Проверка системы
memory_mcp check

# Двухуровневая индексация (сессии + сообщения + задачи + Markdown отчёты)
# При повторном запуске индексируются только новые сообщения!
memory_mcp index --progress

# Управление прогрессом инкрементальной индексации
memory_mcp indexing-progress                    # показать прогресс всех чатов
memory_mcp indexing-progress --chat "ChatName"  # прогресс конкретного чата
memory_mcp indexing-progress --reset            # сбросить прогресс (для полной переиндексации)

# Обновление отчетов без полной индексации
memory_mcp update-summaries                     # обновить все отчеты
memory_mcp update-summaries --chat "ChatName"   # обновить отчет конкретного чата

# Автоматическое ревью саммаризаций
memory_mcp review-summaries --dry-run           # просмотр что будет исправлено
memory_mcp review-summaries                     # исправить все файлы -needs-review
memory_mcp review-summaries --chat "ChatName"   # исправить файлы конкретного чата

# Поиск по сообщениям
memory_mcp search "криптовалюты"

# Поиск по задачам
memory_mcp search "TODO встреча" --collection tasks

# Поиск по сессиям
memory_mcp search "обсуждение проекта" --collection sessions

# Индексация конкретного чата
memory_mcp index --scope chat --chat "Crypto News"

# Полная переиндексация (все сообщения заново)
memory_mcp index --force-full

# MCP сервер с новым инструментом index_chat
### MCP Trading Memory Tools

| Tool | Description |
|------|-------------|
| `store_trading_signal` | Сохраняет торговый сигнал в общую память |
| `search_trading_patterns` | Ищет сигналы по символам/типам/контексту |
| `get_signal_performance` | Возвращает показатели эффективности сигнала |

python scripts/mcp_server.py  # запуск MCP сервера

# Или через скрипт (рекомендуется)
./scripts/run_mcp_local.sh    # запуск MCP сервера локально

# Генерация конфигурации MCP клиента
./scripts/generate_mcp_config.sh  # создание конфигурации для Cursor

# Использование через MCP клиент:
# index_chat("Believers Community", force_full=True, recent_days=0)

# Граф знаний
memory_mcp insight-graph --graphml ./graph.graphml

# Статистика
memory_mcp stats
```

## 📋 Команды

### Основные команды

| Команда | Описание |
|---------|----------|
| `memory_mcp check` | Проверка системы и подключений |
| `memory_mcp index` | **Двухуровневая индексация** (L1: сессии, L2: сообщения, L3: задачи)<br/>✨ Автоматически инкрементальная при повторных запусках |
| `memory_mcp indexing-progress` | Управление прогрессом инкрементальной индексации |
| `memory_mcp update-summaries` | **NEW!** Обновление markdown-отчетов без полной индексации |
| `memory_mcp review-summaries` | **NEW!** Автоматическое ревью и исправление саммаризаций |
| `memory_mcp search "query"` | Поиск по индексированным данным (messages/sessions/tasks) |
| `memory_mcp insight-graph` | Построение графа знаний и анализ связей |
| `memory_mcp stats` | Статистика системы |

### MCP инструменты (NEW!)

| Инструмент | Описание |
|------------|----------|
| `index_chat` | **NEW!** Индексация конкретного чата с отслеживанием прогресса |
| `search_messages` | Поиск по сообщениям с векторными эмбеддингами |
| `search_sessions` | Поиск по саммаризациям сессий |
| `search_tasks` | Поиск по извлечённым задачам и action items |
| `get_chat_info` | Получение информации о конкретном чате |
| `analyze_chat_content` | Анализ содержимого чата с токенизацией |

## 🏗️ Архитектура

```
src/memory_mcp/
├── core/                   # Основная функциональность
│   ├── indexer.py          # 🆕 Двухуровневый индексатор
│   ├── manager.py          # Главный менеджер
│   ├── ollama_client.py    # Клиент Ollama
│   └── mcp_server.py       # MCP сервер
├── analysis/               # Анализ данных
│   ├── session_segmentation.py     # Сегментация сессий
│   ├── day_grouping.py             # Группировка по дням
│   ├── session_summarizer.py       # Саммаризация сессий
│   ├── entity_extraction.py        # Извлечение сущностей
│   ├── markdown_renderer.py        # Рендеринг Markdown
│   ├── cross_analysis.py           # Кросс-анализ
│   └── insight_graph.py            # Граф инсайтов
├── search/                 # Поиск
│   └── search_summaries.py         # Поиск по саммаризациям
├── cli/                    # CLI интерфейс
│   └── main.py                     # Главный CLI
└── utils/                  # Утилиты
```

### Ключевые компоненты

- **TwoLevelIndexer** - двухуровневая индексация с умной группировкой
- **SessionSegmenter** - сегментация сообщений в сессии
- **DayGroupingSegmenter** - оптимизированная группировка по дням
- **SessionSummarizer** - саммаризация с извлечением инсайтов
- **EntityExtractor** - извлечение сущностей и задач
- **MarkdownRenderer** - создание структурированных отчётов

## 🔧 Разработка

### Настройка окружения

```bash
# Установка зависимостей разработки
make install-dev

# Установка pre-commit хуков
pre-commit install

```

### 🆕 Новые возможности для разработчиков

- **Pre-commit хуки** - автоматическая проверка кода при коммите
- **GitHub Actions** - автоматическое тестирование и проверки
- **Настроенная IDE** - VS Code с рекомендуемыми расширениями
- **Улучшенная структура** - логическая организация файлов и документации

### Запуск тестов

```bash
# Все тесты
make test

# Быстрые тесты
make test-fast

# С покрытием
make test
```

### Форматирование кода

```bash
# Форматирование
make format

# Проверка качества
make lint

# Все проверки
make check
```

### CI/CD

```bash
# Локальный запуск CI
make ci
```

## 📊 Производительность

### Двухуровневая индексация

| Размер дампа | Параметры | Время | Результат |
|--------------|-----------|-------|-----------|
| < 50 чатов | `memory_mcp index` | ~10 мин | Полная индексация + отчёты |
| 50-200 чатов | `memory_mcp index --progress` | ~30 мин | Все уровни + задачи |
| 200+ чатов | `memory_mcp index --scope chat --chat "Name"` | ~5 мин/чат | Выборочная индексация |

### Инкрементальная индексация (NEW!)

Система автоматически отслеживает прогресс и индексирует только новые сообщения:

```bash
# Первый запуск - индексирует последние 7 дней (по умолчанию)
memory_mcp index

# Второй запуск - автоматически индексирует только новые сообщения!
memory_mcp index

# Просмотр прогресса
memory_mcp indexing-progress

# Полная переиндексация (все сообщения заново)
memory_mcp index --force-full

# Сброс прогресса для повторной индексации
memory_mcp indexing-progress --reset
```

**Преимущества:**
- ⚡ В 10-100 раз быстрее при повторных запусках
- 💾 Экономия ресурсов Ollama и CPU
- 🔄 Идеально для автоматизации (cron/scheduler)
- 📊 Прозрачное отслеживание прогресса

📖 Подробнее: [`INCREMENTAL_INDEXING_QUICK_START.md`](INCREMENTAL_INDEXING_QUICK_START.md)

### Оптимизация

- Используйте SSD для ChromaDB
- Увеличьте RAM для Ollama (минимум 8GB)
- Используйте инкрементальную индексацию для больших дампов
- Индексируйте конкретные чаты при необходимости

## 🛠️ Устранение неполадок

### Частые проблемы

1. **Ollama не запущен**
   ```bash
   ollama serve
   ```

2. **Модель не найдена**
   ```bash
   ollama pull gemma3n:e4b-it-q8_0
   ollama pull mxbai-embed-large:latest
   ```

3. **Ошибки памяти**
   ```bash
   # Индексируйте чаты по одному
   memory_mcp index --scope chat --chat "Chat Name"
   ```

4. **CLI не найден**
   ```bash
   pip install -e .
   ```

## 📚 Документация

### Основная документация
- [🚀 Быстрый старт](docs/QUICK_START.md) — быстрое начало работы
- [📚 Справочник команд](docs/HELP.md) — подробное руководство по CLI
- [🏗️ Архитектура](docs/ARCHITECTURE.md) — обзор ключевых компонентов
- [📁 Структура проекта](docs/PROJECT_STRUCTURE.md) — **NEW!** Подробное описание структуры

### Функциональность
- [⚡ Инкрементальная индексация](INCREMENTAL_INDEXING_QUICK_START.md) — **NEW!** Индексация только новых сообщений
- [🧠 Улучшения памяти](docs/MEMORY_IMPROVEMENTS.md) — NEW! Гибридный поиск, importance scoring, memory pruning
- [💡 SearchExplainer](docs/SEARCH_EXPLAINER.md) — NEW! Объяснение результатов поиска
- [📊 Кластеризация сессий](docs/CLUSTERING.md) — NEW! Автоматическая группировка сессий в темы (HDBSCAN)
- [🎯 Индексация чатов](docs/INDEX_CHAT_TOOL.md) — NEW! Индексация конкретных чатов через MCP

### Отчёты и анализы
- [📊 Отчёт по интеграции Magistral](docs/reports/MAGISTRAL_INTEGRATION_REPORT.md) — **NEW!**
- [⚡ Отчёт по оптимизации QWEN3](docs/reports/QWEN3_OPTIMIZATION_REPORT.md) — **NEW!**
- [🔄 Пайплайн обработки](docs/_archive/PIPELINE_EXPLAINED.md) — детальное объяснение всех команд
- [📖 Подробно об инкрементальной индексации](docs/INCREMENTAL_INDEXING.md) — полное руководство с примерами

### Разработка
- [🤝 Руководство по участию](CONTRIBUTING.md) — **NEW!** Как участвовать в разработке
- [📋 Улучшения структуры](docs/STRUCTURE_IMPROVEMENTS.md) — **NEW!** Описание проведённых улучшений
- [🔧 Настройка MCP клиента](docs/MCP_CLIENT_SETUP.md) — **NEW!** Конфигурация для Cursor и других клиентов

## 🎯 Рекомендованный workflow

```bash
# Шаг 1: Первоначальная индексация (всё в одном)
memory_mcp index --progress

# Шаг 2: Регулярные обновления (автоматически инкрементальные!)
memory_mcp index  # индексирует только новые сообщения

# Шаг 3: Поиск по данным
memory_mcp search "криптовалюты"  # по сообщениям
memory_mcp search "TODO встреча" --collection tasks  # по задачам
memory_mcp search "обсуждение" --collection sessions  # по сессиям

# Шаг 4: Граф знаний (опционально)
memory_mcp insight-graph --graphml ./graph.graphml

# Шаг 5: Мониторинг прогресса
memory_mcp indexing-progress  # смотрим, что проиндексировано
```

> 💡 **Совет:** Команда `memory_mcp index` делает всё автоматически: группировку, саммаризацию, извлечение задач, создание отчётов и индексацию! При повторных запусках обрабатываются только новые сообщения.

## 🤝 Участие в разработке

1. Форкните репозиторий
2. Создайте ветку для функции (`git checkout -b feature/amazing-feature`)
3. Зафиксируйте изменения (`git commit -m 'Add amazing feature'`)
4. Отправьте в ветку (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

## 📄 Лицензия

Этот проект лицензирован под MIT License - см. файл [LICENSE](LICENSE) для деталей.

## 🙏 Благодарности

- [Ollama](https://ollama.ai/) - для локальных LLM
- [ChromaDB](https://www.trychroma.com/) - для векторного поиска
- [Click](https://click.palletsprojects.com/) - для CLI интерфейса
- [FastAPI](https://fastapi.tiangolo.com/) - для MCP сервера

## 📞 Поддержка

- 🐛 [Issues](https://github.com/memory_mcp/memory_mcp/issues) - сообщения об ошибках
- 💬 [Discussions](https://github.com/memory_mcp/memory_mcp/discussions) - обсуждения
- 📧 [Email](mailto:team@memory_mcp.dev) - прямая связь

---

<div align="center">
  <strong>Сделано с ❤️ командой Telegram Dump</strong>
</div>
