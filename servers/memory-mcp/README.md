# 🚀 Memory MCP Server

[![Python 3.10+](https://img.shields.io/badge/python-3.10+-blue.svg)](https://www.python.org/downloads/)
[![Code style: black](https://img.shields.io/badge/code%20style-black-000000.svg)](https://github.com/psf/black)
[![Ruff](https://img.shields.io/endpoint?url=https://raw.githubusercontent.com/astral-sh/ruff/main/assets/badge/v2.json)](https://github.com/astral-sh/ruff)
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)

**Memory MCP** — сервер Model Context Protocol для индексации и поиска по унифицированной памяти (Telegram, файлы и другие источники), поддерживающий полнотекстовый и векторный поиск, умную группировку сообщений, автоматическую саммаризацию и построение графа знаний.

## 🔌 Сетевая топология

| Service | Port | Transport | Dependencies |
|---------|------|-----------|--------------|
| `binance-mcp` | `8000` | `stdio` | `redis`, `postgres` |
| `tradingview-mcp` | `8060` | `streamable-http` | `redis`, `postgres`, `binance-mcp`, `memory-mcp` |
| `memory-mcp` | `8050` | `stdio` / `streamable-http` | `redis` (опционально) |
| `shell-mcp` | `8070` | `stdio` | `redis` |
| `backtesting-mcp` | `8082` | `stdio` | `postgres`, `binance-mcp`, `tradingview-mcp` |

> **Примечание:** Memory MCP предоставляет MCP-интерфейс по адресу `0.0.0.0:8050` (HTTP режим) и доступен для других сервисов по `http://memory-mcp:8050`. В stdio режиме работает через стандартный ввод/вывод для интеграции с MCP клиентами.

## ✨ Возможности

### Индексация и управление данными
- 🧠 **Двухуровневая индексация** - умная группировка сообщений в сессии с автоматической саммаризацией
- ⚡ **Инкрементальная индексация** - индексация только новых сообщений при повторных запусках (в 10-100 раз быстрее)
- 🎯 **Индексация чатов** - индексация конкретных чатов с отслеживанием прогресса через MCP API
- 🔄 **Фоновая индексация** - автоматическая периодическая индексация новых данных
- 📊 **Кластеризация сессий** - автоматическая группировка похожих сессий в темы (HDBSCAN)
- 📝 **Автоматическая саммаризация** - создание структурированных Markdown отчётов через LM Studio/Ollama
- ✅ **Извлечение задач** - автоматическое выделение action items и TODO из разговоров
- 🔁 **Отслеживание дублей** - анализ повторяющихся сообщений и их вариаций

### Поиск и аналитика
- 🔍 **Векторный поиск** - поиск по сообщениям, сессиям и задачам с помощью эмбеддингов
- 🚀 **Гибридный поиск** - BM25 + Vector search с Reciprocal Rank Fusion (улучшение precision@10 на +18%)
- 💡 **Smart Search** - интерактивный LLM-assisted поиск с сессиями и обратной связью
- 💡 **SearchExplainer** - объяснение результатов поиска с декомпозицией scores и трассировкой связей
- 🕸️ **Типизированный граф** - граф знаний с сущностями, событиями и связями (5 типов узлов, 8 типов рёбер)
- 🔗 **Кросс-анализ** - поиск корреляций и связей между чатами
- 🧠 **Граф инсайтов** - построение графа знаний и выявление скрытых связей

### Управление памятью
- ⭐ **Importance scoring** - автоматическая оценка важности сообщений (0.0-1.0)
- 🗑️ **Memory pruning** - интеллектуальная очистка памяти на основе eviction scores
- 📈 **Статистика и мониторинг** - детальная статистика системы, тегов и использования памяти

### Интеграция и API
- 🌐 **MCP сервер** - полная интеграция с AI клиентами через Model Context Protocol
- 🎯 **CLI интерфейс** - удобный командный интерфейс для всех операций
- 🔌 **REST API** - HTTP endpoint для интеграции с другими сервисами
- ⚡ **Высокая производительность** - оптимизированная обработка больших дампов

## 📁 Структура проекта

```
memory-mcp/
├── src/memory_mcp/
│   ├── cli/                 # CLI утилиты
│   ├── indexing/            # Индексаторы источников (Telegram и др.)
│   ├── memory/              # Граф памяти, FTS, векторное хранилище
│   ├── mcp/                 # MCP schema, adapters и сервер
│   ├── core/                # Основные компоненты (lmstudio_client, ollama_client)
│   ├── analysis/            # Анализ данных и саммаризация
│   ├── quality_analyzer/    # Анализ качества поиска
│   └── utils/               # Утилиты
├── tests/                   # Тесты
├── scripts/                 # Скрипты и утилиты
├── docs/                    # Документация
├── Dockerfile               # Docker образ
├── run_server.py            # HTTP сервер (FastAPI)
├── mcp_server.py            # Точка входа для HTTP режима
├── pyproject.toml           # Конфигурация проекта
├── README.md                # Пользовательская документация
├── AGENTS.md                # Архитектура и руководство
└── CHANGELOG.md             # История изменений
```

**Примечание:** Файл `docker-compose.mcp.yml` находится в родительском репозитории `infra/` по пути `../../infra/docker-compose.mcp.yml` относительно корня проекта.

📖 **Подробное описание структуры:** [`docs/PROJECT_STRUCTURE.md`](docs/PROJECT_STRUCTURE.md)  
📖 **Архитектура и руководство:** [`AGENTS.md`](AGENTS.md)

## 🚀 Быстрый старт

### Установка

```bash
# Клонирование репозитория
git clone https://github.com/memory_mcp/memory_mcp.git
cd memory_mcp

# Установка в режиме разработки
make install-dev

# Или обычная установка
pip install -e .
```

### Настройка переменных окружения

#### Конфигурация эмбеддингов

Memory MCP поддерживает два способа настройки сервиса эмбеддингов:

**Вариант 1: Использование MEMORY_MCP_EMBEDDINGS_URL (приоритет 1)**
```bash
# Для text-embeddings-inference или других совместимых сервисов
MEMORY_MCP_EMBEDDINGS_URL=http://embeddings:80
```

**Вариант 2: Использование LM Studio (приоритет 2)**
```bash
# LM Studio Server настройки
MEMORY_MCP_LMSTUDIO_HOST=127.0.0.1               # хост LM Studio Server (по умолчанию 127.0.0.1)
MEMORY_MCP_LMSTUDIO_PORT=1234                    # порт LM Studio Server (по умолчанию 1234)
MEMORY_MCP_LMSTUDIO_MODEL=text-embedding-qwen3-embedding-0.6b  # модель для эмбеддингов
MEMORY_MCP_LMSTUDIO_LLM_MODEL=                   # модель LLM для генерации текста (опционально, если не указана, используется Ollama)
```

**Важно:** 
- `MEMORY_MCP_LMSTUDIO_MODEL` используется ТОЛЬКО для эмбеддингов (endpoint `/v1/embeddings`)
- `MEMORY_MCP_LMSTUDIO_LLM_MODEL` используется для генерации текста (endpoint `/v1/chat/completions`)
- Если `MEMORY_MCP_LMSTUDIO_LLM_MODEL` не указана, для генерации текста используется Ollama с моделью `gpt-oss-20b:latest`

**Приоритет конфигурации:**
1. Если установлен `MEMORY_MCP_EMBEDDINGS_URL`, он используется напрямую
2. Если `MEMORY_MCP_EMBEDDINGS_URL` не установлен, используется LM Studio (формируется URL автоматически)

#### Интеграция LangChain (опционально)

Memory MCP поддерживает интеграцию с LangChain фреймворком для улучшения работы с эмбеддингами, LLM, промптами и RAG компонентами.

**Feature flags для включения LangChain компонентов:**
```bash
# Использовать LangChain для эмбеддингов
MEMORY_MCP_USE_LANGCHAIN_EMBEDDINGS=true

# Использовать LangChain для LLM
MEMORY_MCP_USE_LANGCHAIN_LLM=true

# Использовать LangChain ретриверы для поиска
MEMORY_MCP_USE_LANGCHAIN_RETRIEVERS=true

# Использовать LangChain для саммаризации
MEMORY_MCP_USE_LANGCHAIN_SUMMARIZATION=true

# Режим саммаризации LangChain (stuff, map_reduce, refine)
MEMORY_MCP_LANGCHAIN_SUMMARIZATION_MODE=map_reduce
```

**Преимущества LangChain интеграции:**
- Унифицированный интерфейс для разных провайдеров LLM/эмбеддингов
- Готовые компоненты для RAG (гибридные ретриверы, contextual compression)
- Улучшенная работа с промптами (валидация, типизация)
- Готовые цепочки для саммаризации (map-reduce, refine)
- Умные text splitters с сохранением контекста

**Обратная совместимость:**
- По умолчанию все флаги выключены (используются старые реализации)
- LangChain компоненты доступны через feature flags
- Все существующие интерфейсы сохранены

📖 **Подробная документация:** [`AGENTS.md#langchain-интеграция`](AGENTS.md#langchain-интеграция)

#### Другие переменные окружения

```bash
# База данных
MEMORY_MCP_DB_PATH=data/memory_graph.db   # путь к SQLite базе данных (по умолчанию data/memory_graph.db)

# Векторное хранилище (опционально)
MEMORY_MCP_QDRANT_URL=http://qdrant:6333         # URL векторного хранилища Qdrant

# Логирование
MEMORY_MCP_LOG_LEVEL=INFO                 # уровень логирования MCP сервера

# HTTP сервер
MEMORY_MCP_PORT=8050                             # порт HTTP сервера (по умолчанию 8050)
MEMORY_MCP_HOST=0.0.0.0                          # хост для HTTP сервера (по умолчанию 0.0.0.0)
```

📖 **Подробная документация по переменным окружения:** [`AGENTS.md#переменные-окружения`](AGENTS.md#конфигурация-и-запуск)


### Использование

```bash
# Проверка системы
memory_mcp check

# Двухуровневая индексация (сессии + сообщения + задачи + Markdown отчёты)
# При повторном запуске индексируются только новые сообщения!
memory_mcp index

# Управление прогрессом инкрементальной индексации
memory_mcp indexing-progress                    # показать прогресс всех чатов
memory_mcp indexing-progress --chat "ChatName"  # прогресс конкретного чата
memory_mcp indexing-progress --reset            # сбросить прогресс (для полной переиндексации)

# Обновление отчетов без полной индексации
memory_mcp update-summaries                     # обновить все отчеты
memory_mcp update-summaries --chat "ChatName"   # обновить отчет конкретного чата

# Автоматическое ревью саммаризаций
memory_mcp review-summaries --dry-run           # просмотр что будет исправлено
memory_mcp review-summaries                     # исправить все файлы -needs-review
memory_mcp review-summaries --chat "ChatName"   # исправить файлы конкретного чата

# Поиск по сообщениям
memory_mcp search "криптовалюты"

# Поиск по задачам
memory_mcp search "TODO встреча" --collection tasks

# Поиск по сессиям
memory_mcp search "обсуждение проекта" --collection sessions

# Индексация конкретного чата
memory_mcp index --scope chat --chat "Crypto News"

# Полная переиндексация (все сообщения заново)
memory_mcp index --force-full

# Запуск MCP сервера

## Stdio режим (для локальной разработки и MCP клиентов)
python -m memory_mcp.mcp.server  # запуск MCP сервера (stdio режим)

# Или через скрипт (рекомендуется)
./scripts/run_mcp_local.sh    # запуск MCP сервера локально (stdio режим)

## HTTP режим (для Docker/продакшена)
python run_server.py          # запуск HTTP сервера на http://localhost:8050

# Генерация конфигурации MCP клиента
./scripts/generate_mcp_config.sh  # создание конфигурации для Cursor

📖 **Подробная документация по режимам запуска:** [`AGENTS.md#режимы-запуска-mcp-сервера`](AGENTS.md#режимы-запуска-mcp-сервера)

# Граф знаний
memory_mcp insight-graph --graphml ./graph.graphml

# Статистика
memory_mcp stats
```

## 📋 Команды

### Основные команды

| Команда | Описание |
|---------|----------|
| `memory_mcp check` | Проверка системы и подключений |
| `memory_mcp index` | **Двухуровневая индексация** (L1: сессии, L2: сообщения, L3: задачи)<br/>✨ Автоматически инкрементальная при повторных запусках |
| `memory_mcp indexing-progress` | Управление прогрессом инкрементальной индексации |
| `memory_mcp update-summaries` | **NEW!** Обновление markdown-отчетов без полной индексации |
| `memory_mcp review-summaries` | **NEW!** Автоматическое ревью и исправление саммаризаций |
| `memory_mcp search "query"` | Поиск по индексированным данным (messages/sessions/tasks) |
| `memory_mcp insight-graph` | Построение графа знаний и анализ связей |
| `memory_mcp stats` | Статистика системы |

### MCP инструменты

#### Системные
| Инструмент | Описание |
|------------|-------------|
| `health` | Проверка состояния MCP сервера, зависимостей и конфигурации |
| `version` | Получение информации о версии сервера и включённых возможностях |

#### Индексация и управление данными
| Инструмент | Описание |
|------------|-------------|
| `ingest` | Универсальная индексация (source_type='records' для записей, source_type='scraped' для веб-контента) |
| `index_chat` | Индексация конкретного Telegram чата с отслеживанием прогресса |
| `get_available_chats` | Получение списка доступных чатов для индексации |
| `background_indexing` | Управление фоновой индексацией (action='start'/'stop'/'status') |
| `get_statistics` | Статистика системы (type='general'/'tags'/'indexing' для разных типов статистики) |
| `import_records` | Массовый импорт записей из JSON/CSV |
| `export_records` | Массовый экспорт записей в JSON/CSV/Markdown |
| `update_record` | Обновление одной записи (контент, метаданные, теги) |
| `batch_operations` | Универсальные batch операции (operation='update'/'delete'/'fetch') |
| `summaries` | Управление саммаризацией (action='update'/'review') |

#### Поиск и аналитика
| Инструмент | Описание |
|------------|-------------|
| `search` | Универсальный поиск (search_type='hybrid'/'smart'/'embedding'/'similar'/'trading') |
| `search_explain` | Объяснение релевантности результата поиска с декомпозицией scores |
| `get_statistics` | Статистика системы (граф, источники, теги, размер БД, прогресс индексации) |
| `get_timeline` | Временная шкала записей, отсортированная по timestamp |
| `analyze_entities` | Извлечение и анализ сущностей из текста |
| `search_entities` | Поиск сущностей по семантическому сходству |
| `get_entity_profile` | Полный профиль сущности (описание, статистика, связанные сущности) |

#### Граф знаний и навигация
| Инструмент | Описание |
|------------|-------------|
| `graph_query` | Универсальные запросы к графу (query_type='neighbors'/'path'/'related') |
| `build_insight_graph` | Построение графа инсайтов из markdown-саммаризаций |

#### Торговые сигналы
| Инструмент | Описание |
|------------|-------------|
| `store_trading_signal` | Сохранение торгового сигнала в память |
| `search` | Поиск торговых паттернов (search_type='trading') |
| `get_signal_performance` | Метрики производительности торгового сигнала |

#### Дополнительные сервисы
| Инструмент | Описание |
|------------|-------------|
| `generate_embedding` | Прямой вызов сервиса эмбеддингов (для отладки и внешних интеграций) |
| `fetch_record` | Получение полной записи по идентификатору |

📖 **Подробная документация по инструментам:** [`AGENTS.md#mcp-инструменты`](AGENTS.md#mcp-инструменты)

## 🏗️ Архитектура

### Структура компонентов

```
src/memory_mcp/
├── core/                      # Основная функциональность
│   ├── indexer.py             # Двухуровневый индексатор
│   ├── indexing_tracker.py    # Отслеживание прогресса индексации
│   ├── manager.py             # Главный менеджер
│   ├── lmstudio_client.py     # Клиент LM Studio Server
│   └── ollama_client.py       # Клиент Ollama
├── mcp/                       # MCP сервер
│   ├── server.py              # MCP сервер (stdio/HTTP режимы)
│   ├── adapters.py            # Адаптеры для работы с памятью
│   └── schema.py              # Схемы данных (Pydantic модели)
├── memory/                    # Хранилище памяти
│   ├── typed_graph.py         # Типизированный граф знаний (SQLite+NetworkX)
│   ├── vector_store.py        # Векторное хранилище (Qdrant)
│   ├── embeddings.py          # Сервис эмбеддингов
│   └── importance_scoring.py  # Оценка важности и очистка памяти
├── analysis/                  # Анализ данных
│   ├── session_segmentation.py    # Сегментация сессий
│   ├── day_grouping.py            # Группировка по дням
│   ├── session_summarizer.py      # Саммаризация сессий
│   ├── entity_extraction.py       # Извлечение сущностей
│   ├── markdown_renderer.py       # Рендеринг Markdown
│   ├── cross_analysis.py          # Кросс-анализ
│   ├── insight_graph.py           # Граф инсайтов
│   ├── session_clustering.py      # Кластеризация сессий (HDBSCAN)
│   └── incremental_context_manager.py  # Расширенный контекст
├── search/                    # Поиск
│   ├── hybrid_search.py       # Гибридный поиск (BM25 + Vector)
│   ├── smart_search.py        # Smart Search движок
│   ├── search_explainer.py    # Объяснение результатов поиска
│   └── search_summaries.py    # Поиск по саммаризациям
├── indexing/                  # Индексаторы источников
│   └── telegram_indexer.py    # Индексатор Telegram чатов
├── quality_analyzer/          # Анализ качества
│   └── utils/                 # Утилиты анализа качества
├── cli/                       # CLI интерфейс
│   └── main.py                # Главный CLI
└── utils/                     # Утилиты
    └── url_validator.py       # Валидация URL
```

### Ключевые компоненты

#### Индексация
- **TwoLevelIndexer** - двухуровневая индексация с умной группировкой (L1: сессии, L2: сообщения, L3: задачи)
- **SessionSegmenter** - сегментация сообщений в сессии (связанные диалоги, разрыв до 4ч)
- **DayGroupingSegmenter** - оптимизированная группировка по дням для каналов
- **IndexingJobTracker** - отслеживание прогресса и жизненного цикла индексаций

#### Анализ и саммаризация
- **SessionSummarizer** - саммаризация с извлечением инсайтов через LLM
- **EntityExtractor** - извлечение сущностей и задач из текста
- **MarkdownRenderer** - создание структурированных Markdown отчётов
- **SessionClusterer** - автоматическая кластеризация сессий в темы (HDBSCAN)

#### Поиск
- **HybridSearchEngine** - гибридный поиск (BM25 вес 0.4 + Vector вес 0.6) с Reciprocal Rank Fusion
- **SmartSearchEngine** - интерактивный LLM-assisted поиск с сессиями и обратной связью
- **SearchExplainer** - объяснение результатов поиска с декомпозицией scores

#### Хранилище
- **TypedGraphMemory** - типизированный граф знаний (5 типов узлов, 8 типов рёбер)
- **VectorStore** - обёртка над Qdrant для векторного поиска
- **ImportanceScorer** - оценка важности сообщений (0.0-1.0)
- **MemoryPruner** - интеллектуальная очистка памяти на основе eviction scores

## 🔧 Разработка

### Настройка окружения

```bash
# Установка зависимостей разработки
make install-dev

# Установка pre-commit хуков
pre-commit install

```

### 🆕 Новые возможности для разработчиков

- **Pre-commit хуки** - автоматическая проверка кода при коммите
- **GitHub Actions** - автоматическое тестирование и проверки
- **Настроенная IDE** - VS Code с рекомендуемыми расширениями
- **Улучшенная структура** - логическая организация файлов и документации

### Запуск тестов

```bash
# Все тесты
make test

# Быстрые тесты
make test-fast

# С покрытием
make test
```

### Форматирование кода

```bash
# Форматирование
make format

# Проверка качества
make lint

# Все проверки
make check
```

### CI/CD

```bash
# Локальный запуск CI
make ci
```

## 📊 Производительность

### Двухуровневая индексация

| Размер дампа | Параметры | Время | Результат |
|--------------|-----------|-------|-----------|
| < 50 чатов | `memory_mcp index` | ~10 мин | Полная индексация + отчёты |
| 50-200 чатов | `memory_mcp index` | ~30 мин | Все уровни + задачи |
| 200+ чатов | `memory_mcp index --scope chat --chat "Name"` | ~5 мин/чат | Выборочная индексация |

### Инкрементальная индексация

Система автоматически отслеживает прогресс и индексирует только новые сообщения:

```bash
# Первый запуск - индексирует последние 7 дней (по умолчанию)
memory_mcp index

# Второй запуск - автоматически индексирует только новые сообщения!
memory_mcp index

# Просмотр прогресса
memory_mcp indexing-progress

# Полная переиндексация (все сообщения заново)
memory_mcp index --force-full

# Сброс прогресса для повторной индексации
memory_mcp indexing-progress --reset
```




## 🛠️ Устранение неполадок

### Частые проблемы

1. **LM Studio Server не запущен**
   ```bash
   # Убедитесь, что LM Studio запущен и сервер работает на http://127.0.0.1:1234
   # Проверьте настройки сервера в LM Studio
    # Или используйте MEMORY_MCP_EMBEDDINGS_URL для другого сервиса эмбеддингов
   ```

2. **Модель не найдена или ошибка "Model is not llm"**
   
   **Важно:** Модели эмбеддингов в LM Studio работают через endpoint `/v1/embeddings`, а не через `/v1/chat/completions`. 
   Ошибка "Model is not llm" возникает, если вы пытаетесь загрузить модель эмбеддингов как LLM модель.
   
   **Правильная настройка:**
   
   a) **Загрузите модель эмбеддингов в LM Studio:**
   - Откройте LM Studio
   - Перейдите в раздел "Search" и найдите модель `text-embedding-qwen3-embedding-0.6b`
   - Скачайте и загрузите модель
   
   b) **Запустите Local Server в LM Studio:**
   - Перейдите в раздел "Local Server"
   - Убедитесь, что сервер запущен на порту 1234 (или другом, указанном в `MEMORY_MCP_LMSTUDIO_PORT`)
   - **Важно:** Не нужно загружать модель в "Chat" - модель эмбеддингов работает автоматически через API
   
   c) **Проверьте доступность модели:**
   ```bash
   # Проверьте список доступных моделей
   curl http://127.0.0.1:1234/v1/models
   
   # Проверьте работу эмбеддингов (это более надежный способ)
   curl -X POST http://127.0.0.1:1234/v1/embeddings \
     -H "Content-Type: application/json" \
     -d '{"model": "text-embedding-qwen3-embedding-0.6b", "input": "test"}'
   ```
   
   d) **Если модель не найдена:**
   - Убедитесь, что имя модели в `MEMORY_MCP_LMSTUDIO_MODEL` точно совпадает с именем модели в LM Studio
   - Проверьте, что модель загружена (не только скачана, но и доступна для использования)
   - Попробуйте перезапустить LM Studio Server
   
   **Примечание:** Memory MCP автоматически проверяет доступность модели через реальный запрос к `/v1/embeddings`, 
   поэтому даже если модель не отображается в списке `/v1/models`, она может работать для эмбеддингов.

3. **Проблемы с конфигурацией эмбеддингов**
   ```bash
   # Проверьте переменные окружения
   echo $MEMORY_MCP_EMBEDDINGS_URL
   echo $MEMORY_MCP_LMSTUDIO_HOST
   echo $MEMORY_MCP_LMSTUDIO_PORT
   echo $MEMORY_MCP_LMSTUDIO_MODEL
   
   # Приоритет: MEMORY_MCP_EMBEDDINGS_URL > LM Studio переменные
   # Если установлен MEMORY_MCP_EMBEDDINGS_URL, LM Studio переменные игнорируются
   ```

4. **Ошибки памяти**
   ```bash
   # Индексируйте чаты по одному
   memory_mcp index --scope chat --chat "Chat Name"
   ```

5. **CLI не найден**
   ```bash
   pip install -e .
   ```

6. **MCP сервер не запускается**
   ```bash
   # Проверьте переменные окружения
   # Проверьте доступность базы данных
   # Проверьте логи: python -m memory_mcp.mcp.server
   ```

## 📚 Документация

### Основная документация
- [🏗️ Архитектура и руководство](AGENTS.md) — **основная документация** по архитектуре, компонентам, MCP инструментам и конфигурации
- [📋 История изменений](CHANGELOG.md) — подробная история всех изменений проекта
- [🏗️ Архитектура компонентов](docs/ARCHITECTURE.md) — обзор ключевых компонентов системы
- [🔧 Настройка MCP клиента](docs/MCP_CLIENT_SETUP.md) — конфигурация для Cursor и других MCP клиентов
- [📊 Анализ качества](docs/QUALITY_ANALYSIS_ARCHITECTURE.md) — архитектура системы анализа качества поиска

### Функциональность
- [⚡ Инкрементальная индексация](INCREMENTAL_INDEXING_QUICK_START.md) — индексация только новых сообщений
- [🧠 Улучшения памяти](docs/MEMORY_IMPROVEMENTS.md) — гибридный поиск, importance scoring, memory pruning
- [💡 SearchExplainer](docs/SEARCH_EXPLAINER.md) — объяснение результатов поиска с декомпозицией scores
- [📊 Кластеризация сессий](docs/CLUSTERING.md) — автоматическая группировка сессий в темы (HDBSCAN)
- [🎯 Индексация чатов](docs/INDEX_CHAT_TOOL.md) — индексация конкретных чатов через MCP API


## 🎯 Рекомендованный workflow

```bash
# Шаг 1: Первоначальная индексация (всё в одном)
memory_mcp index

# Шаг 2: Регулярные обновления (автоматически инкрементальные!)
memory_mcp index  # индексирует только новые сообщения

# Шаг 3: Поиск по данным
memory_mcp search "криптовалюты"  # по сообщениям
memory_mcp search "TODO встреча" --collection tasks  # по задачам
memory_mcp search "обсуждение" --collection sessions  # по сессиям

# Шаг 4: Граф знаний (опционально)
memory_mcp insight-graph --graphml ./graph.graphml

# Шаг 5: Мониторинг прогресса
memory_mcp indexing-progress  # смотрим, что проиндексировано
```
