# TradingView MCP Server - Архитектура и Руководство

## Обзор проекта

**TradingView MCP** — это MCP сервер для анализа криптовалютного рынка с использованием TradingView Screener API, предоставляющий инструменты для скрининга, технического анализа и поиска торговых возможностей.

## Архитектура

### Структура проекта
```
tradingview-mcp/
├── src/tradingview_mcp/   # Основной код
│   ├── config.py          # Конфигурация и настройки
│   ├── server.py          # Главный MCP сервер
│   ├── core/              # Основная логика
│   │   ├── services/      # Сервисы
│   │   │   ├── indicators.py      # Технические индикаторы
│   │   │   ├── coinlist.py        # Управление списками монет
│   │   │   └── screener_provider.py # Провайдер скринера
│   │   └── utils/         # Утилиты
│   │       └── validators.py      # Валидаторы
│   └── coinlist/          # Списки монет по биржам
│       ├── binance.txt
│       ├── kucoin.txt
│       └── ...
├── run_server.py          # Скрипт запуска
├── examples/              # Примеры использования
├── tests/                 # Тесты
└── pyproject.toml         # Конфигурация проекта
```

### Компоненты системы

#### 1. MCP Server (`server.py`)
- **Назначение**: Главная точка входа MCP сервера
- **Функции**: 
  - Регистрация инструментов (tools)
  - Обработка вызовов инструментов
  - Поддержка stdio и HTTP транспортов
- **Паттерны**: FastMCP, декораторы для инструментов

#### 2. Configuration (`config.py`)
- **Назначение**: Управление конфигурацией приложения
- **Функции**:
  - Загрузка переменных окружения
  - Настройки TradingView API
  - Параметры скрининга
- **Паттерны**: Pydantic Settings, валидация

#### 3. Core Services (`core/services/`)
- **Indicators Service**: Вычисление технических индикаторов
- **Coinlist Service**: Управление списками монет по биржам
- **Screener Provider**: Интеграция с TradingView Screener API
- **Паттерны**: Service Layer, Data Processing

#### 4. Utils (`core/utils/`)
- **Validators**: Валидация входных данных
- **Функции**: Проверка бирж, таймфреймов, параметров
- **Паттерны**: Validation, Sanitization

#### 5. Coinlist (`coinlist/`)
- **Назначение**: Статические списки монет по биржам
- **Функции**: Предзагруженные списки для быстрого доступа
- **Формат**: Текстовые файлы с символами

#### 6. Professional Scanners (`pro_scanners/`)
- **Назначение**: Продвинутые торговые сканеры с риск-менеджментом
- **Архитектура**: Модульная система с адаптерами, фильтрами, и хранилищами
- **Зависимости**: Внешние сервисы для полной функциональности
- **Паттерны**: Service Layer, Adapter Pattern, Repository Pattern

##### Структура pro_scanners:
```
pro_scanners/
├── __init__.py            # Экспорт публичного API
├── service.py            # Главный сервис оркестрации
├── config.py             # Загрузка конфигураций
├── models.py             # Модели данных (сигналы, backtest)
├── profiles.py           # Профили риска (conservative, balanced, aggressive)
├── utils.py              # Вспомогательные функции
├── adapters/             # Адаптеры к внешним источникам данных
│   ├── base.py          # Базовый адаптер (интерфейс)
│   └── binance_mcp.py   # Адаптер к binance-mcp сервису
├── scanners/            # Реализации сканеров
│   ├── base.py         # Базовый сканер с общей логикой
│   ├── momentum.py     # Импульсный сканер
│   ├── mean_revert.py  # Сканер возврата к среднему
│   ├── breakout.py     # Сканер пробоев
│   └── volume_profile.py # Сканер объёмного профиля
├── filters/            # Фильтры контекста и деривативов
│   ├── context.py     # Фильтр торгового контекста
│   └── derivatives.py # Фильтр деривативных данных
├── risk/              # Модули управления рисками
│   ├── calculator.py  # Расчёт размера позиции, стопов, целей
│   └── manager.py     # Управление общим риском
├── backtesting/       # Модуль бэктестинга
│   ├── engine.py     # Движок бэктестинга
│   └── metrics.py    # Расчёт метрик производительности
├── storage/          # Хранилища данных
│   ├── redis_cache.py       # Кэш в Redis
│   ├── postgres_store.py    # Хранилище сигналов в Postgres
│   └── memory_bridge.py     # Мост к memory-mcp сервису
└── alerts/          # Система алертов
    ├── router.py    # Роутинг алертов
    ├── callbacks.py # Callback функции
    └── formatter.py # Форматирование сообщений алертов
```

##### Компоненты профессиональных сканеров:

**1. Service Layer (`service.py`)**
- Оркестрация всех компонентов
- Инициализация зависимостей
- Graceful degradation при недоступности сервисов
- Управление жизненным циклом (startup/shutdown)

**2. Scanners (`scanners/`)**
- **Momentum Scanner**: Обнаружение импульсных движений с EMA/ADX/Volume
- **Mean Revert Scanner**: Поиск возвратов к среднему с RSI/Bollinger Bands
- **Breakout Scanner**: Обнаружение пробоев с подтверждением объёмом
- **Volume Profile Scanner**: Анализ Value Area и Point of Control

**3. Adapters (`adapters/`)**
- **BinanceMCPAdapter**: HTTP клиент для binance-mcp сервиса
  - `fetch_klines()`: Получение свечных данных
  - `fetch_ticker_24h()`: 24-часовая статистика
  - `fetch_derivatives()`: Open Interest, Funding Rate, CVD
- Кэширование запросов через Redis
- Обработка таймаутов и ошибок

**4. Filters (`filters/`)**
- **DerivativesFilter**: Фильтрация по Open Interest, Funding Rate
- **ContextFilter**: Валидация по уверенности, кредитному плечу, стопам

**5. Risk Management (`risk/`)**
- **RiskCalculator**: Расчёт размера позиции на основе ATR
- **RiskManager**: Контроль общего риска портфеля

**6. Storage (`storage/`)**
- **RedisCache**: Кэширование индикаторов, сигналов, сессий
- **PostgresStore**: Персистентное хранение сигналов и метрик
- **TradingMemoryRecorder**: Запись сигналов в memory-mcp

**7. Alerts (`alerts/`)**
- **AlertRouter**: Маршрутизация алертов к halv1-bot
- Асинхронная отправка с retry логикой
- Callback система для обработки результатов

## Контекст выполнения и потоки данных

```
MCP Tool Call
    |
    v
TradingView MCP Server (FastMCP)
    |
    |---> Validation & routing (server.py)
    |        |
    |        v
    |    CacheService (Redis -> in-memory fallback)
    |        |
    |        +--> Cache hit -> response
    |        |
    |        +--> Cache miss
    |                |
    |                v
    |        RateLimiter (token bucket + jitter)
    |                |
    |                v
    |        TradingView APIs (tradingview_ta, tradingview_screener)
    |
    +---> Pro Scanner Service
             |
             +--> Binance MCP (market + derivatives data)
             +--> Redis (signal cache)
             +--> PostgreSQL (signal storage)
             +--> HALv1 alerts / Memory MCP bridge
```

## Инфраструктурные зависимости

| Компонент | Назначение | Поведение при отказе |
|-----------|------------|----------------------|
| TradingView API (`tradingview_ta`, `tradingview_screener`) | Источник технических показателей и скринеров | Инструменты возвращают пустые результаты; фиксируется метрика `tradingview.error_rate`, включается кэширование пустых ответов на 60 секунд |
| Redis (`TRADINGVIEW_MCP_REDIS_URL`) | L0-кэш для результатов сканеров, TTL 60/300 секунд | Автоматический fallback на in-memory кэш, Supervisor фиксирует `Fact:TradingViewDependencyState` со статусом `degraded` |
| PostgreSQL (pro scanners) | Хранилище сигналов и метрик | Professional scanners продолжают работу только с in-memory буферами, без записи истории |
| Binance MCP | Поставщик котировок и деривативов для pro scanners | Pro scanners возвращают пустые списки, факт деградации отправляется в supervisor |
| Supervisor MCP (`TRADINGVIEW_MCP_SUPERVISOR_URL`) | Сбор метрик/фактов | Сервер продолжает работать локально, но телеметрия недоступна |

## Кэширование и rate limiting

- `core/services/cache_service.py` реализует кэш с двумя уровнями: Redis (основной) и in-memory fallback. TTL:
  - Рыночные данные (top gainers, multi changes, Bollinger) — `TRADINGVIEW_MCP_CACHE_TTL_MARKET_SECONDS` (по умолчанию 60).
  - Метаданные (профили сканеров, справочная информация) — `TRADINGVIEW_MCP_CACHE_TTL_METADATA_SECONDS` (по умолчанию 300).
- Статистика кэша (hits/misses/errors) агрегируется и каждые 20 обращений отправляется в Supervisor метрикой `tradingview.cache_hit_ratio`.
- `core/utils/rate_limiter.py` — токен-бакет с джиттером, значения управляются переменными:
  - `TRADINGVIEW_MCP_TV_REQUESTS_PER_MIN` (штрафует общий поток запросов).
  - `TRADINGVIEW_MCP_TV_BURST_LIMIT` (разрешенный всплеск).
- При срабатывании ограничения отправляется метрика `tradingview.rate_limit_wait`, что помогает калибровать лимиты.

## Наблюдаемость и телеметрия

- Модуль `supervisor_reporting.py` отправляет:
  - Метрики: `tradingview.screener_latency`, `tradingview.signals_per_minute`, `tradingview.error_rate`, `tradingview.cache_hit_ratio`, `tradingview.rate_limit_wait`.
  - Факты: запуск/остановка сервера, состояние зависимостей, активные профили сканеров.
- События запуска/остановки (`Fact:TradingViewServerStarted`, `Fact:TradingViewServerStopped`) используют снимок конфигурации (`_build_config_snapshot`).
- Ошибки инструментов фиксируются декоратором `log_tool`, что позволяет отслеживать деградацию конкретных MCP tools.

## Graceful Degradation

| Сценарий | Поведение сервера |
|----------|-------------------|
| Redis недоступен | Переключение на in-memory кэш с теми же TTL, лог предупреждения + факт в Supervisor |
| TradingView API rate limiting | Token bucket заставляет ждать, результаты кешируются, но инструмент не падает (возвращает частичный/пустой ответ) |
| TradingView API недоступен | Результаты пустые, error-rate метрики фиксируют аномалию, клиенты могут повторить позже |
| Binance MCP недоступен | Pro scanners возвращают пустой список, в Supervisor уходит `degraded` состояние адаптера |
| PostgreSQL/Alerts недоступны | Сигналы удерживаются в памяти, алерты помечаются как «deferred» |

## Конфигурация (ключевые переменные)

| Переменная | Назначение | Значение по умолчанию |
|------------|------------|------------------------|
| `TRADINGVIEW_MCP_REDIS_URL` | Подключение к Redis | `redis://localhost:6379/0` |
| `TRADINGVIEW_MCP_REDIS_ENABLED` | Включение Redis-кэша | `true` |
| `TRADINGVIEW_MCP_CACHE_TTL_MARKET_SECONDS` | TTL рыночных данных | `60` |
| `TRADINGVIEW_MCP_CACHE_TTL_METADATA_SECONDS` | TTL метаданных | `300` |
| `TRADINGVIEW_MCP_TV_REQUESTS_PER_MIN` | Глобальный лимит запросов | `90` |
| `TRADINGVIEW_MCP_TV_BURST_LIMIT` | Максимальный всплеск | `20` |
| `TRADINGVIEW_MCP_SUPERVISOR_URL` | Endpoint supervisor-mcp | пусто |
| `TRADINGVIEW_MCP_SUPERVISOR_METRICS_ENABLED` | Включение телеметрии | `false` |
| `TRADINGVIEW_MCP_SUPERVISOR_TIMEOUT` | Таймаут запросов в Supervisor | `2.5` |

## Настройка инфраструктуры для профессиональных сканеров

### Обязательные требования:

#### 1. binance-mcp сервис
**Назначение**: Источник рыночных данных (свечи, тикеры, деривативы)

**Установка:**
```bash
# Клонирование и запуск binance-mcp
git clone https://github.com/your-org/binance-mcp.git
cd binance-mcp
docker-compose up -d
# Или запустите в stdio режиме, если это MCP сервер
```

**Проверка доступности:**
```bash
curl http://localhost:8000/health
```

**Конфигурация** (в `configs/infrastructure.yaml`):
```yaml
binance_mcp:
  url: http://binance-mcp:8000
  timeout: 5
```

**Переменные окружения** (альтернатива):
```bash
export BINANCE_MCP_URL=http://localhost:8000
export BINANCE_MCP_TIMEOUT=5
```

### Опциональные (рекомендуемые) зависимости:

#### 2. Redis
**Назначение**: Кэширование индикаторов, сигналов, сессий

**Установка через Docker:**
```bash
docker run -d --name redis -p 6379:6379 redis:alpine
```

**Конфигурация:**
```yaml
redis:
  host: localhost
  port: 6379
  db: 0
  ttl:
    indicators: 300    # 5 минут
    alerts: 1800       # 30 минут
    sessions: 3600     # 1 час
```

**Переменные окружения:**
```bash
export TRADINGVIEW_REDIS_HOST=localhost
export TRADINGVIEW_REDIS_PORT=6379
export TRADINGVIEW_REDIS_DB=0
```

#### 3. PostgreSQL
**Назначение**: Хранение сигналов, метрик, результатов бэктестов

**Установка через Docker:**
```bash
docker run -d --name postgres \
  -e POSTGRES_DB=tradingview_scanners \
  -e POSTGRES_USER=tradingview \
  -e POSTGRES_PASSWORD=tradingview \
  -p 5432:5432 \
  postgres:15-alpine
```

**Применение схемы:**
```bash
psql postgres://tradingview:tradingview@localhost:5432/tradingview_scanners \
  -f sql/pro_scanners_schema.sql
```

**Конфигурация:**
```yaml
postgres:
  host: localhost
  port: 5432
  database: tradingview_scanners
  user: tradingview
  password: tradingview
  pool_size: 10
```

**Переменные окружения:**
```bash
export TRADINGVIEW_POSTGRES_HOST=localhost
export TRADINGVIEW_POSTGRES_PORT=5432
export TRADINGVIEW_POSTGRES_DATABASE=tradingview_scanners
export TRADINGVIEW_POSTGRES_USER=tradingview
export TRADINGVIEW_POSTGRES_PASSWORD=tradingview
```

#### 4. halv1-bot (опционально)
**Назначение**: Получение торговых алертов

**Конфигурация:**
```yaml
halv1_bot:
  url: http://halv1-dashboard:8001/api/trading-alert
  timeout: 10
```

#### 5. memory-mcp (опционально)
**Назначение**: Запись сигналов в долгосрочную память

**Конфигурация:**
```yaml
memory_mcp:
  url: http://memory-mcp:8050/mcp
  timeout: 15
```

### Docker Compose (All-in-One)

**Файл**: `docker-compose.pro_scanners.yaml`
```yaml
version: '3.8'

services:
  redis:
    image: redis:alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    
  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_DB: tradingview_scanners
      POSTGRES_USER: tradingview
      POSTGRES_PASSWORD: tradingview
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./sql:/docker-entrypoint-initdb.d
    
  binance-mcp:
    # Предполагается что у вас есть образ binance-mcp
    image: binance-mcp:latest
    ports:
      - "8000:8000"
    depends_on:
      - redis

volumes:
  redis_data:
  postgres_data:
```

**Запуск:**
```bash
docker-compose -f docker-compose.pro_scanners.yaml up -d
```

### Проверка статуса инфраструктуры

**1. Проверка через health endpoint:**
```python
# В Python
from tradingview_mcp.pro_scanners import ScannerService

service = ScannerService()
await service.startup()

# Проверка флагов
print(f"Redis: {service._redis_ready}")
print(f"Postgres: {service._postgres_ready}")  
print(f"Binance Adapter: {service._adapter_ready}")
print(f"Alerts: {service._alerts_ready}")
```

**2. Проверка логов:**
```bash
# При запуске сервера в логах должно быть:
# INFO: Service startup status: Redis=True, Postgres=True, Binance=True, Alerts=True

# Если binance-mcp недоступен:
# ERROR: Binance adapter unavailable. Pro scanners will not work without binance-mcp.
```

### Graceful Degradation

Система спроектирована для работы с частичной функциональностью:

| Компонент | Статус | Последствия |
|-----------|--------|-------------|
| binance-mcp | ❌ Недоступен | ❌ Профессиональные сканеры возвращают пустые списки |
| Redis | ❌ Недоступен | ⚠️ Работает, но без кэширования (медленнее) |
| PostgreSQL | ❌ Недоступен | ⚠️ Сканеры работают, но метрики/история недоступны |
| halv1-bot | ❌ Недоступен | ⚠️ Сканеры работают, алерты не отправляются |
| memory-mcp | ❌ Недоступен | ⚠️ Сканеры работают, запись в память отключена |

### Troubleshooting профессиональных сканеров

#### Проблема: Профессиональные сканеры возвращают пустые списки

**Причина**: binance-mcp сервис недоступен

**Решение**:
1. Проверьте что binance-mcp запущен:
   ```bash
   curl http://localhost:8000/health
   ```
2. Проверьте URL в `configs/infrastructure.yaml`
3. Проверьте логи TradingView MCP на наличие:
   ```
   ERROR: Binance adapter unavailable. Pro scanners will not work without binance-mcp.
   ```
4. Если используете Docker, проверьте сетевое имя:
   ```yaml
   binance_mcp:
     url: http://binance-mcp:8000  # Docker service name
   ```
   Для локальной разработки:
   ```yaml
   binance_mcp:
     url: http://localhost:8000
   ```

#### Проблема: pro_scanner_metrics/recent_signals не работают

**Причина**: PostgreSQL недоступен

**Решение**:
1. Проверьте подключение к Postgres:
   ```bash
   psql postgres://tradingview:tradingview@localhost:5432/tradingview_scanners -c "SELECT 1"
   ```
2. Примените схему если не применена:
   ```bash
   psql postgres://tradingview:tradingview@localhost:5432/tradingview_scanners \
     -f sql/pro_scanners_schema.sql
   ```
3. Проверьте credentials в `configs/infrastructure.yaml` или переменных окружения

#### Проблема: Медленная работа сканеров

**Причина**: Redis недоступен, нет кэширования

**Решение**:
1. Запустите Redis:
   ```bash
   docker run -d --name redis -p 6379:6379 redis:alpine
   ```
2. Проверьте подключение:
   ```bash
   redis-cli ping  # Должен вернуть PONG
   ```

#### Проблема: pro_scanner_backtest выдаёт ошибку параметров

**Причина**: symbols передан как строка вместо списка

**Решение**:
```python
# ❌ Неправильно
pro_scanner_backtest(symbols="BTCUSDT", ...)

# ✅ Правильно
pro_scanner_backtest(symbols=["BTCUSDT"], ...)
```

Код автоматически конвертирует строку в список, но лучше передавать список явно.

## Принципы разработки

### 1. Производительность
- **Кэширование**: Кэширование результатов анализа
- **Батчевые операции**: Группировка запросов к API
- **Оптимизация**: Минимизация количества API вызовов
- **Параллелизм**: Параллельная обработка символов

### 2. Надежность
- **Обработка ошибок**: Graceful handling ошибок API
- **Retry логика**: Повторные попытки при сбоях
- **Fallback**: Резервные стратегии при недоступности API
- **Валидация**: Проверка всех входных данных

### 3. Точность
- **Технические индикаторы**: Точные вычисления RSI, MACD, Bollinger Bands
- **Валидация данных**: Проверка корректности данных от API
- **Фильтрация**: Исключение некорректных данных
- **Нормализация**: Приведение данных к стандартному формату

### 4. Расширяемость
- **Модульность**: Независимые компоненты
- **Плагины**: Поддержка новых индикаторов
- **Конфигурация**: Гибкая настройка параметров
- **API**: Программный интерфейс для интеграции

## Инструменты (Tools)

### Стандарт описания инструментов
- **Первая строка**: короткий английский глагол в настоящем времени (≤90 символов).
- **Docstring**: первое предложение служит описанием в `list_tools`, дополнительные детали идут дальше.
- **Стиль**: единый тон для всех сканеров и мета-инструментов, чтобы вывод клиента был предсказуемым.
- **Локация**: описания живут в `tradingview_mcp/server.py` рядом с функциями инструмента.

### Основные категории:

#### 1. Market Analysis (базовые инструменты)
- **`top_gainers`**: Топ растущих монет
- **`top_gainers_batch`**: Несколько пресетов топовых ростовиков за один вызов
- **`top_losers`**: Топ падающих монет
- **`top_losers_batch`**: Несколько пресетов топовых падений
- **`multi_changes`**: Изменения по нескольким таймфреймам
- **`multi_changes_batch`**: Пакетные запросы для разных наборов таймфреймов
- **`coin_analysis`**: Детальный анализ конкретной монеты
- **`coin_analysis_batch`**: Массовый анализ нескольких тикеров

**Статус**: ✅ Работают автономно, не требуют внешних зависимостей

#### 2. Technical Screening (базовые инструменты)
- **`bollinger_scan`**: Сканирование по Bollinger Bands
- **`bollinger_scan_batch`**: Пакетное сканирование нескольких настроек Bollinger
- **`consecutive_candles_scan`**: Поиск паттернов свечей
- **`advanced_candle_pattern`**: Продвинутые паттерны свечей

**Статус**: ✅ Работают автономно, не требуют внешних зависимостей

#### 3. Strategy Tools (базовые инструменты)
- **`trend_breakout_pyramiding`**: Стратегия пробоя тренда
- **`pullback_engine`**: Движок откатов
- **`strategy_candidates`**: Кандидаты для стратегий
- **`scan_strategy_candidates`**: Сканирование по стратегиям

**Статус**: ✅ Работают автономно, не требуют внешних зависимостей

#### 4. Professional Scanners (требуют инфраструктуру)
- **`pro_scanner_profiles`**: Список доступных профилей сканеров
- **`pro_momentum_scan`**: Сканер импульса с фильтрами рисков
- **`pro_mean_revert_scan`**: Сканер возврата к среднему с Bollinger/RSI логикой
- **`pro_breakout_scan`**: Обнаружение пробоев с объёмом
- **`pro_volume_profile_scan`**: Проверка value area/POC контекста
- **`pro_scanner_backtest`**: Историческое тестирование профессиональных сканеров
- **`pro_scanner_metrics`**: Агрегированные метрики из PostgreSQL
- **`pro_scanner_recent_signals`**: Последние сохранённые сигналы
- **`pro_scanner_feedback`**: Запись обратной связи по сигналу
- **`pro_scanner_cache_clear`**: Очистка кэша Redis

**Статус**: ⚠️ **ТРЕБУЮТ ВНЕШНЮЮ ИНФРАСТРУКТУРУ**

**Обязательные зависимости:**
- ✅ **binance-mcp** (http://binance-mcp:8000) - источник рыночных данных
  - Без этого сервиса профессиональные сканеры НЕ РАБОТАЮТ
  - Возвращают пустые списки если недоступен

**Опциональные зависимости (рекомендуется):**
- 🔄 **Redis** (localhost:6379) - кэширование результатов
  - Без Redis: работает, но медленнее
- 💾 **PostgreSQL** (localhost:5432) - хранение сигналов и метрик
  - Без Postgres: `pro_scanner_metrics`, `pro_scanner_recent_signals`, `pro_scanner_feedback` не работают
- 📢 **halv1-bot** - отправка алертов
  - Без halv1-bot: алерты не отправляются, но сканирование работает
- 🧠 **memory-mcp** - запись сигналов в память
  - Без memory-mcp: сигналы не записываются в память, но сканирование работает

#### 5. Meta Tools
- **`health`**: Проверка состояния сервера
- **`version`**: Информация о версии
- **`exchanges_list`**: Список доступных бирж

**Статус**: ✅ Работают всегда

### Паттерны инструментов:
- **Валидация входных данных**: Проверка бирж, таймфреймов, лимитов
- **Декораторы**: `@mcp.tool()` и `@log_tool` для регистрации и логирования
- **Обработка ошибок**: Try-catch с понятными сообщениями
- **Кэширование**: Кэширование результатов для производительности

### Документация инструментов (best practices)
- Явно фиксируйте обязательные и взаимоисключающие параметры (например, `symbol` XOR `symbols`).
- Показывайте примеры вызова с реальными значениями биржи/символа/таймфрейма.
- Для списков и сложных полей (например, фильтры, массивы символов) указывайте ожидаемый формат (`["BTCUSDT", "ETHUSDT"]`).
- Для числовых ограничений (`limit`, `threshold`) приводите допустимый диапазон и примеры.
- Добавляйте подсказки по типичным use cases: как выбрать биржу, как комбинировать индикаторы, как использовать инструмент в стратегиях.

## Технические индикаторы

### Поддерживаемые индикаторы:
- **RSI**: Relative Strength Index
- **MACD**: Moving Average Convergence Divergence
- **Bollinger Bands**: Полосы Боллинджера
- **EMA/SMA**: Экспоненциальные и простые скользящие средние
- **ADX**: Average Directional Index
- **Stochastic**: Стохастический осциллятор
- **ATR**: Average True Range

### Вычисления:
- **Метрики**: Автоматическое вычисление метрик
- **Сигналы**: Генерация торговых сигналов
- **Рейтинги**: Система рейтингов для фильтрации
- **Валидация**: Проверка корректности вычислений

## Конфигурация

### Переменные окружения:
```bash
# TradingView API
TRADINGVIEW_API_KEY=your_api_key
TRADINGVIEW_API_SECRET=your_api_secret

# Настройки скрининга
DEFAULT_EXCHANGE=KUCOIN
DEFAULT_TIMEFRAME=15m
MAX_SYMBOLS_PER_REQUEST=200

# Сервер
HOST=0.0.0.0
PORT=8060
LOG_LEVEL=INFO
DEBUG=false
```

### Настройки производительности:
- **Batch size**: Размер батчей для API запросов
- **Rate limiting**: Ограничения частоты запросов
- **Cache TTL**: Время жизни кэша
- **Timeout**: Таймауты для API запросов

## Тестирование

### Структура тестов:
```
tests/
├── test_indicators.py         # Тесты индикаторов
├── test_coinlist.py          # Тесты списков монет
├── test_screener.py          # Тесты скринера
├── test_validators.py        # Тесты валидаторов
└── test_integration.py       # Интеграционные тесты
```

### Принципы тестирования:
- **Unit тесты**: Для каждого компонента
- **Mock объекты**: Для TradingView API
- **Тесты данных**: Проверка корректности вычислений
- **Интеграционные тесты**: Для критических путей

## Развертывание

### Требования:
- Python 3.10+
- TradingView API доступ
- Достаточно ресурсов для обработки данных

### Установка:

#### Локальная разработка с uv (рекомендуется):
```bash
# Установка uv (если не установлен)
pip install uv

# Синхронизация зависимостей из uv.lock
uv sync

# Запуск в stdio режиме
uv run python run_server.py

# Запуск в HTTP режиме
uv run python run_server.py --host 0.0.0.0 --port 8000 --transport http
```

#### Альтернативная установка через pip:
```bash
# Установка зависимостей
pip install -e .

# Запуск в stdio режиме
python run_server.py

# Запуск в HTTP режиме
python run_server.py --host 0.0.0.0 --port 8000 --transport http
```

### Docker Compose развертывание

#### Пересборка и перезапуск после изменений кода

**⚠️ ВАЖНО**: После внесения изменений в код необходимо пересобрать контейнер и перезапустить сервис:

```bash
# Пересборка контейнера с изменениями кода
docker-compose -f ../../infra/docker-compose.mcp.yml build tradingview-mcp

# Перезапуск сервиса с новым образом
docker-compose -f ../../infra/docker-compose.mcp.yml up -d tradingview-mcp

# Проверка статуса сервиса
docker-compose -f ../../infra/docker-compose.mcp.yml ps tradingview-mcp

# Просмотр логов для диагностики
docker-compose -f ../../infra/docker-compose.mcp.yml logs -f tradingview-mcp
```

#### Полная пересборка всех сервисов

```bash
# Пересборка всех MCP сервисов
docker-compose -f ../../infra/docker-compose.mcp.yml build

# Перезапуск всех сервисов
docker-compose -f ../../infra/docker-compose.mcp.yml up -d

# Проверка статуса всех сервисов
docker-compose -f ../../infra/docker-compose.mcp.yml ps
```

#### Отладка и диагностика

```bash
# Вход в контейнер для отладки
docker-compose -f ../../infra/docker-compose.mcp.yml exec tradingview-mcp bash

# Проверка переменных окружения в контейнере
docker-compose -f ../../infra/docker-compose.mcp.yml exec tradingview-mcp env

# Проверка доступности сервиса
curl http://localhost:8060/health
```

#### Очистка и пересборка с нуля

```bash
# Остановка и удаление контейнеров
docker-compose -f ../../infra/docker-compose.mcp.yml down

# Удаление образов (принудительная пересборка)
docker-compose -f ../../infra/docker-compose.mcp.yml build --no-cache tradingview-mcp

# Запуск с чистого листа
docker-compose -f ../../infra/docker-compose.mcp.yml up -d tradingview-mcp
```

### Docker с uv:
```dockerfile
# syntax=docker/dockerfile:1.6
# Обновленный Dockerfile для tradingview-mcp с использованием uv для управления зависимостями
FROM mcp-base:latest

# Переключение на root для установки пакетов
USER root

# Установка uv если не установлен в базовом образе
RUN pip install uv

# Копируем файлы конфигурации зависимостей
COPY pyproject.toml uv.lock README.md ./

# Копируем исходный код и скрипт запуска
COPY src ./src
COPY run_server.py ./

# Установка зависимостей через uv
RUN uv sync --frozen --no-dev

# Копируем файл конфигурации планировщика
RUN mkdir -p /usr/local/lib/python3.11/site-packages/tradingview_mcp/configs
COPY configs/scanner_scheduler.yaml /usr/local/lib/python3.11/site-packages/tradingview_mcp/configs/scanner_scheduler.yaml

# Исправляем права на директорию виртуального окружения
RUN chmod -R 755 /app/.venv

# Установка переменных окружения
ENV PATH="/app/.venv/bin:${PATH}"

# Переключение обратно на непривилегированного пользователя
USER mcp

# Экспорт порта сервиса
EXPOSE 8000

# Команда запуска сервиса
CMD ["python", "run_server.py", "--host", "0.0.0.0", "--port", "8000"]
```

### Преимущества использования uv:
- **Скорость**: Значительно быстрее установки зависимостей по сравнению с pip
- **Детерминизм**: Использование `uv.lock` обеспечивает воспроизводимые сборки
- **Безопасность**: Проверка целостности зависимостей
- **Оптимизация**: Более эффективное кэширование и параллельная установка

## Мониторинг и логирование

### Логирование:
- **Уровни**: DEBUG, INFO, WARNING, ERROR
- **Контекст**: Биржа, символ, таймфрейм
- **Метрики**: Время выполнения, количество символов

### Мониторинг:
- **Health check**: Статус сервера и API
- **Метрики**: Количество запросов, ошибки API
- **Алерты**: Превышение лимитов, ошибки API

## Безопасность

### Принципы:
1. **API ключи**: Безопасное хранение в переменных окружения
2. **Rate limiting**: Соблюдение лимитов TradingView API
3. **Валидация**: Проверка всех входных данных
4. **Мониторинг**: Отслеживание использования API

### Рекомендации:
- Используйте ограниченные API ключи
- Настройте мониторинг использования API
- Регулярно обновляйте списки монет
- Кэшируйте результаты для снижения нагрузки

## Развитие проекта

### Приоритеты:
1. **Точность**: Улучшение алгоритмов анализа
2. **Производительность**: Оптимизация API запросов
3. **Функциональность**: Новые стратегии и индикаторы
4. **Интеграция**: Поддержка новых бирж

### Roadmap:
- [ ] Поддержка новых бирж
- [ ] Расширенные стратегии
- [ ] Машинное обучение для анализа
- [ ] Веб-интерфейс для визуализации
- [ ] Интеграция с торговыми ботами
- [ ] Реалтайм уведомления

## Документация и отчёты

### ⚠️ ВАЖНО: Правила работы с документацией

**НЕ СОЗДАВАЙТЕ .md файлы отчёты каждый раз!**

- **НЕ создавайте** новые .md файлы для каждого изменения или отчёта
- **НЕ создавайте** файлы типа `REPORT_2024.md`, `CHANGES.md`, `UPDATE.md` и т.п.
- **НЕ дублируйте** информацию в разных файлах

### Ведение changelog:

**ВСЕГДА обновляйте существующие файлы:**

1. **`CHANGELOG.md`** - основной файл для записи изменений
2. **`README.md`** - обновляйте при значительных изменениях
3. **`AGENTS.md`** - обновляйте при изменениях архитектуры
4. **Существующие .md файлы** - дополняйте, не создавайте новые

### Формат changelog:
```markdown
## [Версия] - YYYY-MM-DD

### Добавлено
- Новая функциональность

### Изменено
- Изменения в существующей функциональности

### Исправлено
- Исправления ошибок

### Удалено
- Удаленная функциональность
```

### Принципы:
- **Консолидация**: Вся информация в существующих файлах
- **Обновление**: Редактируйте существующие файлы
- **Структурированность**: Используйте единый формат
- **Отслеживаемость**: Все изменения в changelog

## Качество кода и стандарты

### Code Review
- **Review guidelines**: Обязательный код-ревью для всех изменений
- **Automated checks**: Автоматические проверки через CI/CD
- **Code coverage**: Минимум 80% покрытия кода тестами
- **Algorithm review**: Особое внимание к корректности технических индикаторов

### Документация кода
- **API documentation**: Полная документация всех инструментов анализа
- **Code comments**: Комментарии для сложных математических вычислений
- **Type hints**: Обязательные аннотации типов для всех функций
- **Docstrings**: Строки документации для всех публичных методов

### Стандарты кодирования
- **PEP 8**: Строгое соблюдение стандартов Python
- **Black**: Автоматическое форматирование кода (длина строки 88)
- **Ruff**: Линтинг кода с правилами E, F, I
- **MyPy**: Строгая проверка типов (strict mode)

### Инструменты качества

#### Через uv (рекомендуется):
```bash
# Форматирование кода
uv run black .

# Линтинг
uv run ruff check .

# Проверка типов
uv run mypy .

# Тестирование
uv run pytest tests/ --cov=src --cov-report=html

# Проверка безопасности
uv run bandit -r src/
uv run safety check
```

#### Через pip (альтернатива):
```bash
# Форматирование кода
black .

# Линтинг
ruff check .

# Проверка типов
mypy .

# Тестирование
pytest tests/ --cov=src --cov-report=html

# Проверка безопасности
bandit -r src/
safety check
```

### Управление зависимостями
- **uv add [package]**: Добавление новой зависимости
- **uv remove [package]**: Удаление зависимости
- **uv sync**: Синхронизация зависимостей из lock файла
- **uv lock**: Обновление lock файла при изменении pyproject.toml

### Принципы разработки
- **SOLID принципы**: Следование принципам объектно-ориентированного дизайна
- **DRY**: Не повторяйся - избегание дублирования кода
- **KISS**: Простота и понятность кода
- **Accuracy first**: Точность вычислений превыше всего

### Структура коммитов
- **Conventional Commits**: Использование стандартного формата
- **Prefixes**: `feat:`, `fix:`, `docs:`, `test:`, `refactor:`, `perf:`, `analytics:`
- **Scope**: Указание области изменений
- **Breaking changes**: Явное указание критических изменений

### Примеры коммитов:
```bash
feat(indicators): добавить новый технический индикатор ADX
fix(screener): исправить расчет Bollinger Bands
docs(api): обновить документацию по стратегиям
test(indicators): добавить тесты для RSI вычислений
refactor(coinlist): оптимизировать загрузку списков монет
perf(api): оптимизировать батчевые запросы к TradingView
analytics(strategy): улучшить алгоритм поиска паттернов
```
