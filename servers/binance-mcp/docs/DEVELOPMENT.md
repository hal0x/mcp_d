# Руководство по разработке

## 🚀 Быстрый старт

1. **Клонируйте репозиторий:**
```bash
git clone <repository-url>
cd binance-mcp
```

2. **Настройте окружение для разработки:**
```bash
make dev-setup
```

3. **Настройте переменные окружения:**
```bash
cp .env.example .env
# Отредактируйте .env файл
```

## 🛠️ Команды разработки

### Основные команды
```bash
make help          # Показать все доступные команды
make install       # Установить зависимости
make install-dev   # Установить зависимости для разработки
make run           # Запустить MCP сервер
make test          # Запустить тесты
make test-cov      # Запустить тесты с покрытием
make lint          # Проверить код линтерами
make format        # Форматировать код
make clean         # Очистить временные файлы
```

### Примеры и утилиты
```bash
make examples      # Запустить примеры использования
make check-permissions  # Проверить права API
make utils         # Запустить утилиты
```

## 📁 Структура проекта

```
binance-mcp/
├── src/                    # Основной код
│   ├── __init__.py
│   ├── api.py             # API клиент
│   ├── client.py          # Binance клиент
│   ├── config.py          # Конфигурация
│   ├── models.py          # Модели данных
│   └── services.py        # Бизнес-логика
├── tests/                 # Тесты
│   ├── __init__.py
│   ├── conftest.py       # Конфигурация pytest
│   └── test_*.py         # Тесты
├── examples/              # Примеры использования
├── scripts/               # Утилиты
├── docs/                  # Документация
└── .github/               # GitHub Actions
```

## 🧪 Тестирование

### Запуск тестов
```bash
# Все тесты
make test

# С покрытием кода
make test-cov

# Только unit тесты
pytest -m unit

# Только integration тесты
pytest -m integration
```

### Написание тестов
- Тесты должны быть в папке `tests/`
- Имена файлов: `test_*.py` или `*_test.py`
- Используйте фикстуры из `conftest.py`
- Покрытие кода должно быть > 80%

## 🔧 Форматирование и линтинг

### Автоматическое форматирование
```bash
make format
```

### Проверка стиля
```bash
make lint
```

### Pre-commit хуки
Pre-commit хуки настроены автоматически при `make install-dev`:
- Удаление trailing whitespace
- Фиксация end-of-file
- Проверка YAML
- Форматирование с black
- Сортировка импортов с isort
- Линтинг с flake8
- Проверка типов с mypy

## 📝 Стиль кода

### Python
- Используйте Black для форматирования (длина строки 88)
- Используйте isort для сортировки импортов
- Следуйте PEP 8
- Используйте type hints
- Документируйте функции и классы

### Импорты
```python
# Стандартная библиотека
import os
import sys

# Сторонние библиотеки
import requests
from pydantic import BaseModel

# Локальные импорты
from src.models import AccountInfo
from src.services import AccountService
```

### Документация
- Используйте docstrings для всех функций и классов
- Формат: Google style
- Примеры:
```python
def get_account_info() -> AccountInfo:
    """Получить информацию об аккаунте.
    
    Returns:
        AccountInfo: Информация об аккаунте Binance.
        
    Raises:
        BinanceAPIException: При ошибке API.
    """
```

## 🚀 CI/CD

### GitHub Actions
- Автоматические тесты на Python 3.11 и 3.12
- Проверка линтеров и форматирования
- Проверка безопасности
- Загрузка покрытия кода

### Локальная проверка
```bash
# Проверить все перед коммитом
make lint
make test
make format
```

## 🐛 Отладка

### Логирование
```python
import logging

logger = logging.getLogger(__name__)
logger.info("Информационное сообщение")
logger.error("Ошибка: %s", error_message)
```

### MCP Inspector
```bash
npx @modelcontextprotocol/inspector python mcp_server.py
```

## 📦 Управление зависимостями

### Добавление новой зависимости
1. Добавьте в `pyproject.toml` в секцию `dependencies`
2. Для dev зависимостей - в `[project.optional-dependencies.dev]`
3. Обновите `uv.lock`:
```bash
uv lock
```

### Обновление зависимостей
```bash
uv lock --upgrade
```

## 🔒 Безопасность

### API ключи
- Никогда не коммитьте `.env` файлы
- Используйте `.env.example` как шаблон
- Ограничьте права API ключей
- Регулярно обновляйте ключи

### Проверка безопасности
```bash
# Проверка уязвимостей
safety check

# Анализ безопасности кода
bandit -r src
```

## 📚 Полезные ссылки

- [Python Best Practices](https://docs.python-guide.org/)
- [Pydantic Documentation](https://pydantic-docs.helpmanual.io/)
- [FastAPI Documentation](https://fastapi.tiangolo.com/)
- [MCP Documentation](https://modelcontextprotocol.io/)
- [Binance API Documentation](https://binance-docs.github.io/apidocs/)
