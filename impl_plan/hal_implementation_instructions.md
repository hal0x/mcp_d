# 📘 Инструкции для AI‑агента: «План реализации — Доведение HAL до полноценного AI‑агента»

> Этот документ — операционное руководство для автономного агента (HALv1) и его разработчиков. Здесь описаны цели, минимальный состав модулей, строгие интерфейсы, пошаговые задачи, критерии готовности и правила безопасности.

---

## 0. Цели и принципы

**Цели:**
1. Стабилизировать HALv1 и унифицировать взаимодействие с MCP‑серверами.
2. Ввести **supervisor‑mcp** (наблюдение, реестр, метрики, факты) и **learning‑mcp** (обучение на телеметрии) как первоочерёдные приоритеты.
3. Достичь управляемой автономности: планирование → dry‑run → policy‑check → подтверждение/авто‑режим → исполнение → логирование → обучение.

**Принципы:** Safety > Reliability > Accuracy > Helpfulness > Performance > Autonomy.

---

## 1. Минимальный состав модулей (v1)
- **HALv1 Core** — планирование/исполнение, Telegram интерфейс.
- **supervisor‑mcp** — реестр всех MCP, health, сбор метрик и фактов, агрегаты, алерты.
- **learning‑mcp** — чтение агрегатов из supervisor, вычисление и предложение новых профилей решений.
- **policy/config** — единая точка политик, фича‑флагов и профилей решений.
- **memory‑mcp** — SSOT для фактов (Signal/Decision/Trade/Outcome) и контекста.

---

## 2. Арт‑объекты и контракты (строго)

### 2.1. Событие‑факт (единица истории)
```json
{
  "ts": "2025-10-19T12:05:00Z",
  "kind": "Fact:Outcome|Fact:Trade|Fact:Decision|Fact:Signal|Fact:PolicyHit",
  "actor": "halv1",
  "correlation_id": "tsk_abcd1234",
  "payload": { /* произвольно, но сериализуемо */ }
}
```

### 2.2. Метрика
```json
{"ts":"2025-10-19T12:00:00Z","name":"rpc_latency_p95","value":173,
 "tags":{"svc":"binance-mcp","op":"create_order"}}
```

### 2.3. DecisionProfile (версионируемая конфигурация)
```json
{
  "profile_id": "default_v1.3",
  "weights": {"squeeze": 0.38, "trend_up": 0.24, "funding_neg": 0.16, "low_corr": 0.22},
  "thresholds": {"score_trade": 0.67, "score_full": 0.80},
  "caps": {"max_risk_pct": 0.8, "max_change_per_epoch": 0.05},
  "valid_until": "2025-11-01T00:00:00Z"
}
```

---

## 3. API (черновик)

### 3.1. supervisor‑mcp
**Discovery**
- `GET /mcp/registry` → список MCP (имя, протокол, версия, эндпоинт).
- `GET /mcp/{name}/capabilities` → инструменты, схемы, квоты.

**Health**
- `GET /health` → сводный статус.
- `GET /health/{name}` → метрики конкретного MCP.

**Ingest**
- `POST /ingest/metric {name, ts, value, tags}` (батч допускается)
- `POST /ingest/event {Fact:*}`

**Query**
- `GET /query/metrics?name=…&from=…&to=…`
- `GET /query/agg?kind=business&window=7d`
- `GET /query/facts?kind=Fact:Trade&from=…`

**Alerts**
- `POST /alerts/rules` (CRUD), `GET /alerts/active`

### 3.2. learning‑mcp
- `POST /train/offline?window=30d` → рассчитать дельты весов/порогов на основе агрегатов supervisor.
- `POST /propose` → выдать **кандидат DecisionProfile** + пояснение/guardrails.
- `POST /ab/start {profile_id, budget_pct, duration}` → старт A/B.
- `POST /ab/stop {profile_id}` → остановка/оценка A/B.
- `POST /promote {profile_id}` → запрос на промо (через policy/config).
- `GET /profiles/latest` → список активных/кандидатов с метриками.

### 3.3. HALv1 ↔ policy/config
- `GET /decision/profile` с `If-None-Match: <etag>` (клиентский кэш).
- `POST /decision/profile/apply {profile_id}` (только sandbox при авто‑режиме).

---

## 4. Пошаговые задачи (операционный план)

### Sprint A (Недели 1–2): Supervisor v1 (ingest + query)
1. Создать схемы БД: `metrics(time,name,tags,value)`, `facts(time,kind,actor,correlation_id,jsonb)`.
2. Реализовать `/ingest/metric`, `/ingest/event` (батчи, ретраи, идемпотентность по `(ts,name,tags)`/`correlation_id`).
3. Реализовать `/query/*` (метрики, агрегаты 7d/30d).
4. Добавить health‑проверки MCP и `/health`.
5. HALv1: отправка 6 базовых метрик и Fact:* после каждого действия.

**Критерии готовности:** ingest > 99% success, p95 ingest < 150 мс, агрегаты корректны на тестовых данных.

### Sprint B (Недели 3–4): Learning v1 (offline+propose)
1. Собрать из supervisor агрегаты: `expectancy_7d`, `hit_rate_7d`, `maxDD_30d`, фичи решений.
2. Реализовать `train/offline` (EMA‑обновление весов/порогов, guardrails).
3. Реализовать `propose` (DecisionProfile + rationale + caps).
4. Policy: ввести ETag/версионирование профилей, ограничение `max_change_per_epoch`.
5. HALv1: чтение DecisionProfile (кэш), фича‑флаг `decision_profile_id`.

**Критерии готовности:** корректные дельты весов на синтетике; propose → валидируемый профиль.

### Sprint C (Недели 5–6): A/B + алерты + безопасность
1. Реализовать `ab/start|stop` с бюджетом ≤ 1% капитала.
2. Supervisor: алерты (`rpc_success_rate`, `latency_p99`, `drawdown`, `expectancy_7d<0`).
3. Policy: обязательный dry‑run и подтверждение для всех side‑effects вне sandbox.
4. Телеметрия A/B в supervisor, карточки результатов для Telegram.

**Критерии готовности:** промо кандидата допускается только при `expectancy_B − expectancy_A ≥ +0.05` и `maxDD_B ≤ maxDD_A + 0.5%`.

---

## 5. Стандарты ошибок и ретраев (единый шаблон RPC)
- **Формы ответа:** `Ok{data}`, `Err{code,msg,details}`, `Meta{latency,tokens}`.
- **Ретраи:** exp‑backoff 250–2000 мс, 3 попытки;
- **Circuit‑breaker:** open при error‑rate>p95, half‑open 30 с;
- **Fallback:** кэш/нооп/дефолтный профиль;
- **Escalate:** алерт в supervisor + пауза действия при критике.

---

## 6. Тестирование
- **Unit:** сериализация DTO, валидация схем, расчёт агрегатов, EMA‑логика.
- **Интеграционные:** HALv1→supervisor ingest; learning→supervisor query; policy‑apply; A/B роутинг.
- **Нагрузочные:** 10× текущей скорости событий в течение 24 ч, ноль потерь.
- **Отказоустойчивость:** падение MCP, network split, таймауты — корректное «деградирование» и восстановление.

---

## 7. Безопасность и аудит
- Secrets в Vault/ENV (никогда в логах).
- Роли: `view|train|promote|trade`; Telegram‑списки доверенных chat_id.
- 2‑key подтверждение для операций > X USD.
- Полный audit trail: кто/когда/почему; связываем `Fact:*` с профилем решений.

---

## 8. Эксплуатация
- Blue/Green деплой критичных модулей; авто‑rollback по метрикам.
- SLO: ingest success ≥ 99%, p95 query < 200 мс, MTTR < 15 мин.
- Runbooks: reconnect MCP, restore snapshot, rotate keys, pause all.

---

## 9. Критерии «готово» (Definition of Done)
- HALv1 стабилен: нет зависаний 48 ч, все e2e‑сценарии зелёные.
- supervisor‑mcp: корректно собирает метрики и факты, отдаёт агрегаты и health.
- learning‑mcp: ежедневно предлагает профиль, соблюдая guardrails; A/B работает.
- Решения агента объяснимы (rationale) и воспроизводимы по фактам.

---

## 10. Мини‑чек‑лист запуска
- [ ] Включены отправка метрик/фактов из HALv1.
- [ ] supervisor: registry/health/query/alerts работают.
- [ ] learning: train/offline + propose выдают валидные дельты.
- [ ] policy: ETag‑профили, dry‑run, ограничения риска.
- [ ] TG‑бот: карточки A/B и подтверждений.
- [ ] Backups: snapshot профилей/фактов/метрик.

