# 🧠 План реализации: Доведение HAL до полноценного AI агента

## 🚀 Этап 0 — Базовая платформа (текущее состояние)
**Компоненты:**
- HALv1 (ядро агента)
- Memory MCP (память и контекст)
- TradingView MCP (аналитика и сигналы)
- Binance Native MCP (исполнение сделок)
- Shell MCP (скрипты и системные команды)
- MCP Web UI (визуализация и отладка)

**Цель:** связать все MCP в единую архитектуру с понятным жизненным циклом задач.

---

## ⚙️ Этап 1 — Централизация и оркестрация (1–2 недели)

### 1. Orchestrator MCP
- Реализовать FSM задач: `Draft → Planned → DryRun → AwaitConfirm → Executing → Done`.
- Поддержать паузы, отмену, повтор, дедлайны.
- Встроить idempotency key и retry/backoff.
- Интеграция с Telegram (кнопки подтверждения, отчёты, пауза задач).

### 2. Policy MCP
- Централизованные правила и лимиты для действий:
  - `allow/deny` для MCP вызовов.
  - `require_confirm_for` — действия, требующие подтверждения.
  - Ограничения по плечу, риску и notional.
- Локальный кэш политик в Redis.

### 3. Logging MCP
- Унифицированный формат событий: `Fact:Signal`, `Fact:Decision`, `Fact:Trade`, `Fact:Outcome`.
- Отправка логов в Memory MCP (контекстная память).
- Поддержка query API для ретроспективного анализа.

---

## 🧩 Этап 2 — Самостоятельное принятие решений (2–3 недели)

### 4. Decision MCP
- Модуль анализа торговых возможностей:
  - Вход: сигналы TradingView MCP + контекст портфеля.
  - Выход: оценка `score`, `risk_pct`, `SL/TP`, объяснение (`explain`).
- Алгоритм: rule-based + linear weighting.
- Подключение к Orchestrator для автоматического планирования.

### 5. Learning MCP
- Контур обратной связи (reinforcement loop):
  - Анализ закрытых сделок, адаптация параметров стратегий.
  - Метрики: expectancy, Sharpe, hit-rate, drawdown.
  - Оптимизация параметров через contextual bandit (ε-greedy).

### 6. Data Integration MCP
- Единый фичестор и ETL:
  - Интеграция данных TradingView, Binance, Memory.
  - Проверка качества: полнота, свежесть, выбросы.
  - Версионирование семплов для обучения.

---

## 🔍 Этап 3 — Мониторинг и самоконтроль (3–4 недели)

### 7. Supervisor MCP
- Контроль двух плоскостей:
  - **Торговая:** маржа, экспозиции, риск, drawdown.
  - **Агентская:** latency MCP, success-rate, SLA.
- Алерты и автодействия: «Pause all», «Reduce leverage», «Close worst positions».

### 8. Security MCP
- Авторизация (по chat_id / роли).
- Журнал аудита действий.
- Поддержка режимов `sandbox` и `prod`.

### 9. Backup MCP
- Автоматическое создание snapshot’ов состояния:
  - Memory + Decision + Policy + Config.
  - Хранение в S3/MinIO.
  - Автоматическое восстановление по ключу.

---

## 💬 Этап 4 — Telegram агент (4–5 недель)

### 10. Natural Language Router
- Распознавание интентов (регексы + LLM router).
- Сопоставление фраз → MCP-вызовы (через Orchestrator).
- Контекстная память: предыдущие запросы, предпочтения пользователя.

### 11. Диалоговая логика
- Тред-мастера для задач (FSM внутри чата).
- Dry-run + подтверждение перед действиями.
- Отчёты и алерты в формате Markdown.

### 12. Обучение на диалогах
- Memory MCP хранит NL-запросы → действия → результаты.
- Learning MCP анализирует успешность и корректирует маршрутизацию интентов.

---

## 🧠 Этап 5 — Автономность и самообслуживание (5–7 недель)

### 13. Self-Monitoring & Recovery
- Автоисправление при сбоях: перезапуск MCP, восстановление индексов.
- Проверка здоровья всех модулей (health-checks + автоперезапуск).
- Оптимизация параметров оркестратора (таймауты, max retries).

### 14. Meta-Reasoner
- Слой объяснения: анализ принятых решений, выявление ошибок.
- Генерация кратких саммари («почему купил», «что сработало», «что улучшить»).
- Визуализация reasoning path в Web UI.

### 15. Evolution Loop
- Автоматическая генерация гипотез на основе результатов.
- Тестирование новых стратегий в sandbox с ограниченным капиталом.
- Автопереход успешных стратегий в prod.

---

## 🏁 Этап 6 — Завершение и интеграция (7–8 недель)

### 16. Unified Dashboard (Web UI)
- Единая панель состояния агентов, стратегий и рисков.
- Граф связей между MCP.
- Интерактивный отладчик задач FSM.

### 17. Central Config MCP
- Единая точка конфигурации (env, токены, лимиты, параметры моделей).
- Версионирование конфигураций и hot reload.

### 18. Deployment Automation
- Docker Compose / Kubernetes.
- Локальные и облачные среды.
- CI/CD пайплайн с проверкой MCP схем и тестов.

---

## 📈 Финальное состояние: HAL Autonomous AI Agent

| Подсистема | Назначение | MCP |
|-------------|-------------|-----|
| **Оркестрация** | FSM и планирование задач | orchestrator-mcp |
| **Память** | Контекст, логи, факты | memory-mcp |
| **Решения** | Оценка и выбор действий | decision-mcp |
| **Обучение** | Самоадаптация стратегий | learning-mcp |
| **Мониторинг** | Контроль рисков и SLA | supervisor-mcp |
| **Интеграция данных** | Фичестор и валидация | data-integration-mcp |
| **Безопасность** | Роли и аудит | security-mcp |
| **Логирование** | История событий | logging-mcp |
| **Конфигурация** | Централизованные параметры | config-mcp |
| **Интерфейс** | Telegram / Web / Voice | tg-bot, web-ui |

---

## 🧩 Дополнительно
- Все MCP — gRPC/HTTP совместимые микросервисы.
- Redis для кэша и очередей задач.
- Prometheus + Grafana для мониторинга.
- Минимум 1 резервная копия состояния в сутки.

---

### ✅ Критерий «полнейшего агента»
1. Может анализировать, планировать, действовать и объяснять решения.
2. Сам адаптируется под рынок и внутренние сбои.
3. Имеет целостное состояние, восстанавливаемое из snapshot.
4. Обеспечивает прозрачность и контроль из Telegram/Web.
5. Готов к масштабированию под новые MCP (новые домены/инструменты).



---

# 🔍 Критический анализ и правки плана

Ниже — исправления и дополнения по результатам ревью рисков: учтены техдолг HALv1, миграция, безопасность, мониторинг, тестирование, ресурсы и реалистичные сроки.

## 🧯 Этап 0.5 — **Стабилизация основы** (2–3 недели, до Оркестратора)
**Цели:** убрать системные подвисания и несогласованность модулей, повысить предсказуемость.
- Починить `test_multi_step_task` (причина: гонки в планировщике/esper EventBus).  
  Действия: lock-free очередь + таймауты на `join()` + watchdog.
- Развязать AgentCore и executor: явный контракт «план → шаг → побочный эффект → артефакт».
- Ввести «heartbeat» для всех лонг-ранning задач; автоматический cancel по TTL.
- Повысить покрытие: unit ≥ 85% (критические узлы 100%), интеграционные e2e ≥ 25 сценариев.
- Профилирование: p95 latency, p99 stalls. Фикс «долгих GC/IO».

**Критерии выхода:** все тесты зелёные, p95<1.5× от базового, ни одного hang за 48 ч нагрузочного прогона.

---

## 🔁 План миграции (обратная совместимость и откаты)
1. **Шлюз совместимости** (`mcp-proxy`): единый фасад поверх текущих MCP; нормализация схем (request/response, ошибки, idempotency).
2. **Инкрементальная интеграция Orchestrator:**
   - S0: только read-only/сухие прогоны (DryRun);
   - S1: частичное управление (non-critical ops);
   - S2: полный контроль с флагом `require_confirm`.
3. **Миграция данных:** миграционные задачи в `data-integration-mcp` (версии `v_current → v1`), dual-write на период 2–4 недели.
4. **План отката:** каждое изменение конфигурируется флагом; откат — через переключение на предыдущий фасад + восстановление snapshot.

---

## 🧭 Система приоритетов и принципов
- **Приоритеты:** Safety > Reliability > Accuracy > Helpfulness > Performance > Autonomy.
- **Fail-Safe:** любой side‑effect только после `policy.check` + `dry_run` + (если требуется) `confirm`.
- **Stop-Rules:** глобальный «красный флаг»: `drawdown>threshold` или `error_rate>p99` → автоматическая пауза торговых действий.

---

## ⏱ Пересборка сроков (реалистично)
Общий горизонт: **16–20 недель** (4 релиза по ~1 месяц с буфером 25%).
- R1 (Нед. 1–4): Этап 0.5 + Orchestrator v1 (DryRun‑only) + Policy v1 + Мониторинг v0.  
- R2 (Нед. 5–8): Decision v1 (rule-based) + Logging v1 + TG мастера + Sandbox trading.
- R3 (Нед. 9–12): Supervisor v1 (риск/маржа), Security v1 (ролевая модель), Data-Integration v1, частичная автоторговля.
- R4 (Нед. 13–16+): Learning v1 (bandit), Security v2 (2‑key), Prod‑режим с автопромо стратегий, Backups v1.

---

## 🧪 Стратегия тестирования
- **Unit:** контракты MCP, сериализация, политика, скоринг.  
- **Интеграционные:** сквозные сценарии FSM (Draft→Done/Failed), ошибки сети/таймауты, дубли Callback.
- **Нагрузочные:** RPS профили, bursts, долговременные тесты 24–48 ч.
- **Отказоустойчивость:** kube‑kill/latency injection, network partition, crash‑recovery.
- **Canary:** новые версии MCP через трафик 5→25→100%.

---

## 🔒 Безопасность — с **R1**, а не с R3
- Secrets через Secret Manager/ENV sealed; нулевой лог ключей.
- Ролевая модель Telegram: `view|trade|scripts`, белые списки chat_id.
- `policy-mcp`: лимиты риска/плеча/notional, торговые окна, запрет на маржинальные действия в sandbox.
- 2‑key подтверждение для операций > X USD.

---

## 📈 Мониторинг — с первого релиза
- **Торговый контур:** маржа, экспозиции, корреляции, drawdown, SLA алертов.
- **Агентский контур:** latency/timeout/error‑rate по MCP, очередь задач Оркестратора, stalled‑детектор.
- Каналы алертов: Telegram + Web UI; действия: `pause_all`, `reduce_leverage`, `close_worst`.

---

## 🧮 Ресурсы и масштабирование
- План мощности: CPU/RAM/IO на MCP; лимиты и запросы.  
- Кэш: Redis (idempotency, rate‑limits, session state).  
- Горизонтальное масштабирование Orchestrator/Decision; очереди для batch‑задач (Learning/Backtests).  
- Хранилище: S3/MinIO для snapshot/логов/артефактов; ротация и TTL.

---

## 🔄 План отката (per‑feature)
- Фича‑флаги, версионирование схем, «двойная запись».  
- Snapshot/restore для Memory/Decision/Policy/Config.  
- Документированные playbooks: «как откатить R1/R2…», чек‑листы.

---

## 📚 Документация и UX
- DevDocs: контракты MCP, примеры, коды ошибок.  
- UserDocs: сценарии в Telegram, глоссарий терминов, FAQ.  
- API Docs: автоген по `mcp-proxy`.  
- UX: единый стиль ответов в ТГ, «пояснить решение» одной кнопкой.

---

## 🛠 Операции и поддержка
- On‑call график/рутину, приоритет инцидентов (P0..P3).  
- Чёткие SLO/SLA: p95 latency, alerting MTTA/MTTR.  
- Регулярные post‑mortem и RCA.

---

## 📏 Метрики успеха (Definition of Done by release)
| Релиз | Критерии |
|---|---|
| **R1** | 0 подвисаний за 48ч; unit≥85%; p95<1.5×; DryRun‑трейды с подтверждением; базовый мониторинг работает |
| **R2** | ≥5 e2e сценариев в sandbox; decision‑score согласуется с ручной оценкой ≥80%; логирование Fact:* полно |
| **R3** | Управление риском из ТГ; canary‑деплой без регрессий; Security v1 в проде |
| **R4** | Learning улучшает utility на ≥10% QoQ; автопромо стратегий с безопасными пределами |

---

## ✅ Изменения в дорожной карте (tl;dr)
1) Добавлен **Этап 0.5** (стабилизация).  
2) Безопасность и мониторинг сдвинуты в **R1**.  
3) Введены миграция/откат, стандартизация схем через `mcp-proxy`.  
4) Сроки увеличены ×2, добавлен буфер 25%.  
5) Определены метрики успеха и stop‑rules.



---

# 🔍 Вторичный критический анализ: Углублённые риски и новые меры

## 🚨 Новые критические недочёты

### 1. Сложность интеграции MCP-серверов
**Риск:** Разные протоколы (stdio, HTTP), форматы и модели ошибок.  
**Меры:**
- Создать **MCP Integration Layer** поверх `mcp-proxy`, обеспечивающий:
  - Авто-нормализацию входных/выходных форматов (JSON schema registry).
  - Circuit breaker + retry + fallback на уровне RPC.
  - Health aggregator (раз в 30 с проверяет все MCP).  
- Разделить MCP по уровням надёжности: critical / optional.

### 2. Сложность ML и Learning MCP
**Риск:** Недостаток данных и риск деградации модели.  
**Меры:**
- Этап **Data Bootstrapping** (2–3 недели) до Learning MCP:
  - Сбор исторических данных стратегий и сигналов.
  - Введение baseline: ручная торговля vs AI стратегия.
  - A/B‑тестирование стратегий с ограничением капитала ≤1% портфеля.
  - Валидация моделей: MAE, win‑rate, cumulative return.
  - Авто‑откат при деградации метрик.

### 3. Обработка edge‑cases и отказов
**Риск:** Одновременные сбои MCP, частичные отказы, разрывы связи.  
**Меры:**
- **Circuit breaker per MCP** (задержка + fallback cache).  
- **Graceful degradation:** при недоступности TradingView использовать кэш сигналов из Memory MCP.  
- **Auto‑resume:** очередь невыполненных задач при восстановлении сети.

### 4. Координация и конфликты между MCP
**Риск:** Разногласия между Decision, TradingView и Learning MCP.  
**Меры:**
- Ввести **Arbitration Engine** в Orchestrator:
  - Каждое решение проходит голосование: `Decision → Policy → Learning`.  
  - Конфликты решаются по приоритету (Policy > Decision > Learning).
  - Learning может рекомендовать, но не отменять ордер без подтверждения Policy.

### 5. Изоляция компонентов
**Риск:** Каскадные сбои между MCP.  
**Меры:**
- Контейнеризация каждого MCP с лимитами CPU/RAM/IO.  
- Введение **Task timeout isolation**: ни один вызов не блокирует другие.  
- Memory MCP в отдельном процессе с rate‑limit.

### 6. Приоритеты операций
**Риск:** Нечёткий баланс между безопасностью, скоростью и автономностью.  
**Меры:**
- Приоритеты действий:
  1. Безопасность (policy, auth, limits)
  2. Надёжность (повтор/откат)
  3. Контроль (confirm)
  4. Автономия (self‑exec)
  5. Производительность

---

## ⏱ Коррекция сроков (реалистично)
- **Полный цикл:** 36–45 недель (3 фазы × 3 месяца + буфер).  
- **Этап 0.25 – Валидация архитектуры (1–2 недели):** интеграция 2–3 MCP, тест производительности, проверка безопасности.
- **Этап 0.5 – Стабилизация HALv1 (3–4 недели):** устранение багов, нормализация API, база тестов.
- **R1 (6–8 недель):** Orchestrator + Policy + Monitoring.
- **R2 (8–10 недель):** Decision + Data Integration + Logging + Security.
- **R3 (12–14 недель):** Learning + Automation + Sandbox→Prod + финальные тесты.

---

## 💰 Ресурсы и масштабирование
- GPU/TPU узел для Learning MCP (минимум 1×RTX A5000 экв.).  
- RAM ≥ 32 ГБ для Memory MCP.  
- Auto‑scale по CPU для Decision MCP.
- S3/MinIO архив для логов, TTL 30 дней.

---

## 🔒 Безопасность и аудит
- Security MCP подключается **до R1**.  
- Secrets хранить через Vault; все торговые ключи в зашифрованном хранилище.  
- Audit trail для каждой торговой операции (время, пользователь, стратегия, результат).  
- Pen‑тест раз в квартал.

---

## 🧪 Тестирование и валидация
- **Интеграционные тесты:** после каждого релиза, сценарии «MCP‑A down», «network split», «partial order».  
- **Performance:** stress‑тесты до 10× текущей нагрузки.  
- **Security:** fuzzing + попытки SQL/HTTP‑инъекций.
- **AI‑валидация:** baseline vs AI, A/B тесты, drift‑детектор.

---

## 📊 Мониторинг бизнес‑метрик
- ROI стратегий, cumulative return, maxDD, hit‑rate.  
- Confidence Decision MCP, learning gain per epoch.  
- User satisfaction (опросы, feedback).  
- Alert: "ROI<0 три дня подряд" или "learning_gain<0.01".

---

## 🧭 Операции и поддержка
- **24/7 on‑call график**, SLA: MTTA ≤15 мин, MTTR ≤60 мин.  
- Runbooks для типовых сбоев: reconnect MCP, restore snapshot, rotate keys.  
- Rolling‑обновления без простоя: 2× ноды с blue‑green деплоем.

---

## ✅ Финальные дополнения к плану
1. Добавлен **Этап 0.25 — Валидация архитектуры**.  
2. Расширен **Этап 0.5 — Стабилизация HALv1**.  
3. Введён **Integration Layer** и **Arbitration Engine**.  
4. Добавлены планы на A/B тестирование и baseline AI.  
5. Обновлены сроки (до 45 недель) и ресурсы.  
6. Добавлены метрики ROI и AI‑валидации.  
7. Включён план поддержки 24/7 и runbooks.



---

# 🧱 Валидация архитектуры: учтённый критический анализ

**Итоговая оценка текущей архитектуры:** 7.5/10. Ниже — закреплённые решения по четырём ключевым проблемам и их встраивание в дорожную карту.

## 1) Интеграция между HALv1 и MCP
**Проблема:** разные протоколы (stdio/HTTP), модели ошибок и форматы.  
**Решение:** **MCP Integration Layer** поверх `mcp-proxy`.
- JSON Schema Registry для нормализации request/response; автоген валидации.
- Unified RPC: retry(3), backoff(250–2000 мс), circuit‑breaker (half‑open 30 с).
- Health Aggregator: активные health‑пробы всех MCP, сводка статусов.
- Классы надёжности: `critical|standard|optional` (орchestrator учитывает деградацию).

**Артефакты:** `integration/contract/*.json`, `integration/clients/*.py`, `integration/tests/*`.

## 2) Зависания многошаговых задач / EventBus
**Проблема:** `test_multi_step_task` подвисает, `bus.join()` блокируется.  
**Решение:**
- Таймауты и watchdog для `join()`; cancel‑tokens на шаги FSM.  
- Lock‑free очередь + backpressure; отдельные execution‑воркеры.  
- Circuit‑breaker на Executor; идемпотентность шагов (step_id + TTL).

**Критерии:** ни одного hang за 48 ч под нагрузкой; e2e 25 сценариев green.

## 3) Централизованная конфигурация
**Проблема:** разноформатные конфиги (yaml/json/py).  
**Решение:** **Config MCP** (single source of truth) с hot‑reload.
- API: `get/set/reload`, валидация по схемам, версионирование.
- Политика «двойной записи» на миграционный период.

## 4) Система памяти
**Проблема:** дублирование HALv1 memory и Memory MCP.  
**Решение:** унификация вокруг Memory MCP + локальный L0‑кэш.
- Контракт `Fact:*` + `Session:*`; единые индексы L1/L2.
- HALv1 → Memory MCP через адаптер, локальный кэш для быстрых чтений.

---

## Встраивание в Roadmap (скорректировано)

### Этап 0.25 — **Архитектурная валидация** (1–2 недели)
- Прототип `MCP Integration Layer` на 2–3 серверах (TradingView, Binance, Memory).
- Тесты производительности и отказов (circuit‑breaker, fallback).
- Безопасность с реальными ключами в песочнице.

### Этап 0.5 — **Стабилизация HALv1** (3–4 недели)
- Fix EventBus/Executor (watchdog, timeouts, идемпотентность).
- Health‑пробы, backpressure, воркеры.
- База тестов: unit≥85%, e2e≥25, 48 ч soak без зависаний.

### R1 (6–8 недель)
- Orchestrator v1 (Dry‑run only) + Policy v1 + Supervisor v0.  
- **MCP Integration Layer v1**: schema‑registry, unified errors, health‑агрегатор.  
- **Config MCP v1** (get/set/reload), начало миграции конфигов.

### R2 (8–10 недель)
- Decision v1 (rule‑based) + Data Integration v1 + Logging v1 + Security v1.  
- **Memory унификация** (HALv1↔Memory MCP), L0‑кэш.  
- TG‑мастера: подтверждения, объяснимость решений.

### R3 (12–14 недель)
- Learning v1 (baseline + A/B, кап. ≤1% портфеля) + Sandbox→Prod.  
- Supervisor v1 (риск/маржа/SLA), Blue‑Green деплой, 24/7 on‑call.

> Общая длительность программы: 36–45 недель (с буферами).

---

## Контрольные метрики (архитектурная готовность)
- **HALv1 Core:** p95 latency, stalled=0, e2e pass rate ≥ 95%.
- **Integration Layer:** success‑rate RPC ≥ 99%, MTTR < 10 мин, error‑taxonomy покрыто ≥ 90% случаев.
- **Config MCP:** 100% компонентов читают из центра, hot‑reload без рестартов.
- **Memory унификация:** доля обращений через Memory MCP ≥ 95%.

---

## Операционные практики
- Runbooks: reconnect MCP, restore snapshot, rotate keys, circuit‑trip.  
- Canary & rollback по фича‑флагам.  
- Audit trail: кто/когда/почему → воспроизводимые решения.  
- Приоритеты: **Safety > Reliability > Accuracy > Helpfulness > Performance > Autonomy**.



---

# 🧭 Supervisor MCP — единый «надсмотрщик» экосистемы

**Роль:** единый реестр и наблюдатель за всеми MCP. Даёт HALv1 знание о доступных возможностях и состоянии системы, а Learning MCP — агрегированные данные для обучения.

## Задачи Supervisor MCP
1) **Discovery & Registry**: список MCP, их версии, доступные инструменты (capabilities), схемы и ограничения.
2) **Health & Status**: периодические health‑пробы, SLA/latency/error‑rate, статусы `up|degraded|down`.
3) **Metrics & Facts Store**: сбор и хранение техметрик и бизнес‑фактов (`Fact:Signal/Decision/Trade/Outcome/PolicyHit`).
4) **Aggregation & Query**: агрегаты за 7/30 дней, KPI для принятия решений и обучения.
5) **Alerts & Actions**: правила алертов и рекомендации действий (Pause All, Reduce leverage, Close worst N).

## API (черновик)
### Discovery
- `GET /mcp/registry` → список MCP, версии, эндпоинты, протоколы.
- `GET /mcp/{name}/capabilities` → инструменты, схемы, ограничения, квоты.

### Health
- `GET /health` → сводный статус.
- `GET /health/{name}` → метрики MCP.

### Ingest
- `POST /ingest/metric` → `{name, ts, value, tags}` (батч).
- `POST /ingest/event` → `Fact:*` события.

### Query
- `GET /query/metrics?name=…&from=…&to=…`
- `GET /query/agg?kind=business&window=7d`
- `GET /query/facts?kind=Fact:Trade&from=…`

### Alerts
- `POST /alerts/rules` (CRUD)  
- `GET /alerts/active`

## Схемы данных (минимум)
**Metric**
```json
{"ts":"2025-10-19T12:00:00Z","name":"rpc_latency_p95","value":173,
 "tags":{"svc":"binance-mcp","op":"create_order"}}
```
**Fact**
```json
{"ts":"2025-10-19T12:05:00Z","kind":"Fact:Outcome","actor":"halv1",
 "correlation_id":"tsk_abc123",
 "payload":{"symbol":"TONUSDT","strategy":"pullback_engine","r_multiple":1.3,
            "decision_features":{"squeeze":1,"trend_up":1,"funding_neg":1,"low_corr":1}}}
```

## Как используют другие
- **HALv1**: читает `/mcp/registry` и `/health` чтобы знать, *что можно* и *что сейчас работает*; использует агрегаты для принятия решений об исполнении.
- **Learning MCP**: читает `/query/agg` и `/query/facts` для обучения; пишет рекомендации обратно через policy/config.

## Миграция с Monitoring → Supervisor
- Переименовать сервис и маршруты, сохранить совместимость ingest API.
- Добавить `Discovery` и `Registry` поверх существующих таблиц.
- Расширить алерты бизнес‑правилами (ROI, expectancy, drawdown).

