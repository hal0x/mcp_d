# Скрипты запуска и настройки

Эта директория содержит все скрипты для запуска и настройки HALv1.

> **📚 Документация по оптимизации и памяти** теперь находится в [docs/OPTIMIZATION_AND_MEMORY_STRATEGY.md](../docs/OPTIMIZATION_AND_MEMORY_STRATEGY.md)

## 🚀 Скрипты запуска

### `run_colima.sh`
Скрипт для запуска HALv1 с Colima (macOS).
- Автоматически запускает Colima с оптимальными настройками для Apple Silicon
- Создает .env файл с базовой конфигурацией
- Запускает Docker Compose

### `run_docker.sh`
Скрипт для запуска HALv1 с Docker.
- Проверяет доступность Docker
- Создает .env файл
- Запускает Docker Compose

### `auth.sh`
Скрипт для настройки аутентификации.
- Настройка Telegram API
- Настройка LLM провайдеров

## 🐳 Docker файлы

### `Dockerfile`
Основной Docker образ для HALv1.

### `docker-compose.yml`
Конфигурация Docker Compose для запуска всех сервисов.

### `.dockerignore`
Файлы и директории, исключаемые из Docker образа.

## 📋 Использование

```bash
# Запуск с Colima (macOS)
./scripts/run_colima.sh

# Запуск с Docker
./scripts/run_docker.sh

# Настройка аутентификации
./scripts/auth.sh
```

## 🔧 Требования

- Docker или Colima (macOS)
- Python 3.11+
- Telegram Bot Token
- LLM провайдер (LM Studio по умолчанию; Ollama и др. опционально)
