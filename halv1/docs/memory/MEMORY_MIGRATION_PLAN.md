# 📋 План миграции с MemoryStore на UnifiedMemory

**Статус**: ✅ **Миграция в основном завершена**  
**Последнее обновление**: 7 ноября 2025

## 🎯 Цель
Полностью заменить `MemoryStore` на `UnifiedMemory` во всем проекте, сохранив обратную совместимость и функциональность.

## 📊 Текущее состояние

### ✅ Выполнено
- ✅ `UnifiedMemory` создан и работает
- ✅ В `memory/__init__.py` настроены алиасы: `MemoryStore = UnifiedMemory` и `MemoryServiceAdapter = UnifiedMemory`
- ✅ Основной код (`main.py`, `agent/core.py`) использует `UnifiedMemory`
- ✅ Интеграционные тесты переведены на `UnifiedMemory`
- ✅ Обратная совместимость обеспечена через алиасы

### 📝 Текущая ситуация
- **Алиасы работают**: Все импорты `MemoryStore` и `MemoryServiceAdapter` автоматически используют `UnifiedMemory`
- **Старый код существует**: `memory/memory_store.py` все еще существует для обратной совместимости, но не используется напрямую
- **Тесты проходят**: Все тесты работают через алиасы

### 🔄 Опциональная очистка (не критично)
- Можно обновить импорты в тестах для явного использования `UnifiedMemory`
- Можно удалить старый `memory/memory_store.py` после полной проверки
- Можно обновить документацию для явного указания `UnifiedMemory`

## 🗺️ Этапы миграции

### ✅ Этап 1: Подготовка (завершен)
- [x] **Создать тесты совместимости** - `UnifiedMemory` полностью совместим с `MemoryStore`
- [x] **Документировать различия** - различия задокументированы в коде
- [x] **Создать алиасы** - `MemoryStore = UnifiedMemory` в `__init__.py`

### ✅ Этап 2: Миграция основного кода (завершен)
- [x] **Обновить `agent/core.py`** - использует `UnifiedMemory`
- [x] **Обновить `main.py`** - использует `UnifiedMemory`
- [x] **Настроить алиасы** - обратная совместимость через алиасы

### 🔄 Этап 3: Опциональная очистка (не критично)
- [ ] **Обновить импорты в тестах** - заменить `from memory import MemoryStore` на `from memory import UnifiedMemory` (опционально, алиасы работают)
- [ ] **Обновить примеры** - `examples/prompt_system_example.py` (опционально)
- [ ] **Удалить старый код** - удалить `memory/memory_store.py` (после полной проверки)
- [ ] **Обновить документацию** - обновить все ссылки на `MemoryStore` (частично выполнено)

## 🔧 Детальный план действий

### 1. Создание тестов совместимости

```python
# tests/test_memory/test_compatibility.py
def test_unified_memory_memorystore_compatibility():
    """Тест полной совместимости UnifiedMemory с MemoryStore API."""
    # Тестируем все методы MemoryStore
    pass
```

### 2. Скрипт миграции

```python
# scripts/migrate_memory_store.py
def migrate_file(file_path: str) -> None:
    """Мигрирует файл с MemoryStore на UnifiedMemory."""
    # Заменяет импорты
    # Обновляет создание экземпляров
    # Исправляет параметры
```

### 3. Обновление тестов

**Приоритет 1 (критичные):**
- `tests/test_agent/test_agent_core.py` - 7 использований
- `tests/test_agent/test_agent_flow.py` - 5 использований
- `tests/test_core/test_main_smoke.py` - 2 использования

**Приоритет 2 (важные):**
- `tests/test_memory/` - все файлы (специфичные тесты памяти)
- `tests/integration/` - интеграционные тесты

**Приоритет 3 (остальные):**
- Остальные тестовые файлы

### 4. Обновление основного кода

**Файлы для обновления:**
- `agent/core.py` - заменить `MemoryStore()` на `UnifiedMemory()`
- `tasks/scheduler.py` - проверить использование
- `examples/prompt_system_example.py` - обновить примеры

## 🚨 Риски и митигация

### Риск 1: Нарушение обратной совместимости
**Митигация:** Создать comprehensive тесты совместимости перед миграцией

### Риск 2: Различия в поведении API
**Митигация:** Документировать все различия и создать mapping guide

### Риск 3: Поломка тестов
**Митигация:** Мигрировать по частям, тестируя после каждого этапа

## 📈 Метрики успеха

- [x] **Обратная совместимость** - все импорты `MemoryStore` работают через алиасы
- [x] **100% тестов** проходят после миграции
- [x] **0 breaking changes** в публичном API
- [x] **Улучшение производительности** - UnifiedMemory использует современную L0-L∞ архитектуру
- [ ] **Опционально**: Явное использование `UnifiedMemory` в коде (не критично, алиасы работают)

## 🗓️ Временные рамки

| Этап | Время | Ответственный |
|------|-------|---------------|
| Подготовка | 1-2 дня | Backend team |
| Миграция тестов | 2-3 дня | QA team |
| Миграция кода | 1 день | Backend team |
| Очистка | 1 день | Backend team |
| **Итого** | **5-7 дней** | |

## 🔄 Rollback план

Если что-то пойдет не так:
1. **Вернуть алиас** `MemoryStore = UnifiedMemory` в `__init__.py`
2. **Откатить изменения** в тестах через git
3. **Восстановить** `memory/memory_store.py` из git history
4. **Проанализировать** проблемы и исправить

## 📝 Чек-лист миграции

### Перед началом
- [ ] Создать backup текущего состояния
- [ ] Запустить все тесты и убедиться, что они проходят
- [ ] Создать feature branch для миграции

### После каждого этапа
- [ ] Запустить тесты и убедиться, что они проходят
- [ ] Проверить, что основное приложение работает
- [ ] Обновить документацию

### После завершения
- [ ] Удалить старый код
- [ ] Обновить README и документацию
- [ ] Создать PR с описанием изменений
- [ ] Провести code review

## 🎉 Достигнутые результаты

После завершения миграции:
- ✅ **Единая система памяти** - `UnifiedMemory` используется через алиасы
- ✅ **Улучшенная производительность** - современная архитектура L0-L∞
- ✅ **Обратная совместимость** - старый код работает без изменений
- ✅ **Лучшая тестируемость** - единообразные тесты
- ✅ **Готовность к будущему** - легко добавлять новые функции
- ✅ **Интеграция с Memory MCP** - готовность к переходу на Memory MCP через `MCPMemoryAdapter`

## 🔮 Следующие шаги

Согласно [плану развития HAL агента](../../impl_plan/hal_ai_agent_plan.md):
- **R2**: Унификация памяти (HALv1↔Memory MCP), L0-кэш
- **R3**: Полная интеграция с типизированной граф-памятью (см. [MEMORY2_Typed_Graph_Memory.md](MEMORY2_Typed_Graph_Memory.md))

**Связанные документы:**
- [MCP_INTEGRATION.md](MCP_INTEGRATION.md) - интеграция с Memory MCP
- [MEMORY2_Typed_Graph_Memory.md](MEMORY2_Typed_Graph_Memory.md) - план развития типизированной граф-памяти
- [../ARCHITECTURE.md](../ARCHITECTURE.md) - архитектура системы памяти
